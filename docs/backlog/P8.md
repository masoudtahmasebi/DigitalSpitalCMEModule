# Epic P8 · Certificates & email — 7 h

**Week 5.** The learner-visible proof of participation. By design (ADR-0005) the
certificate does **not** depend on a successful EIV submission — that separation
is what makes the credentials-late fallback viable.

**Every task in this epic is behind the human review gate.**

> **Do not start this phase until S12 is answered.** The Teilnahmebescheinigung
> Muster states the certificate _"ist nur vollständig ausgefüllt und mit
> **Originalstempel** … gültig"_. If the ÄKWL reads that strictly, an emailed PDF
> with an embedded stamp image is not a valid document and this entire phase
> produces nothing usable. See `docs/show-stoppers.md` S12.
>
> **Dependency:** the signature/stamp asset must be supplied by MEDICE before this
> phase starts (roadmap §12 item 3). The ADHS SMTP configuration file is needed
> for P8-02 (roadmap §12 item 4).
>
> **Re-estimate pending.** The Anerkennungsbescheid adds `Anschrift` and two VNR
> barcodes to the required content (S13), roughly **+4 h** on a 7 h phase. Not
> folded into the estimates below until the scope decision in S4/S13 is taken.

---

### P8-01 · Certificate PDF generation

- **jira:** _(unassigned)_
- **Estimate:** 2.5 h · **Week:** 5 · **Depends on:** P4-05
- **Review gate:** human — certificates

**Context**
A CME certificate is a document a physician submits to their Landesärztekammer.
Every field on it is a factual claim about an accredited event, so a wrong value
is a compliance problem rather than a formatting defect.

**Scope**

The Bescheid states the minimum set verbatim: _"Veranstaltungsnummer (VNR),
-titel, -datum, -uhrzeit, -ort, Veranstalter, Punkte und Kategorie sowie den
Namen des Teilnehmenden"_, stamped and signed by the Wissenschaftliche Leitung.

- PDF from the ÄKWL template carrying: VNR, Veranstaltungstitel, **Datum and
  Uhrzeit** (both, from the completion timestamp), Veranstaltungsort,
  Veranstalter, Punkte und Kategorie, participant name, and the MEDICE
  signature/stamp asset.
- The mandatory sentence, as a template with substitutions rather than a fixed
  string — a course with different accreditation must not emit "4 Punkten
  (Kategorie D)":
  > Die Veranstaltung ist im Rahmen der Zertifizierung der ärztlichen Fortbildung
  > der Ärztekammer Westfalen-Lippe mit 4 Punkten (Kategorie D) anrechenbar.
- **From the Muster, pending the S13 decision:** `Anschrift` and the VNR rendered
  as both **Code 39** and **DataMatrix** ("Felder bitte nicht überkleben").
- Values read from the completion record and the enrolment snapshot, never from
  the live course record.
- Deterministic: regenerating a certificate for the same completion produces the
  same document.

**Acceptance criteria**

- [ ] Every field in the Bescheid's minimum set is present, including Uhrzeit
- [ ] The mandatory sentence renders the course's own points and category, never
      a hardcoded "4 Punkten (Kategorie D)"
- [ ] Every field matches the completion record and enrolment snapshot exactly
- [ ] A later change to the course's CME points does not alter an already-issued
      certificate
- [ ] Regeneration is byte-stable for the same completion
- [ ] German characters, long names and long course titles render without
      truncation or overflow
- [ ] The certificate is issued on completion regardless of EIV submission status

---

### P8-02 · Project-bound SMTP configuration

- **jira:** _(unassigned)_
- **Estimate:** 1.5 h · **Week:** 5 · **Depends on:** P2-01
- **Review gate:** human — certificates

**Context**
Roadmap §2: SMTP settings come from the existing ADHS platform PHP configuration
and are bound per project, so each customer's mail leaves from their own sender
rather than a shared one.

**Scope**

- Encrypted-at-rest SMTP credentials on the project record, seeded from the
  handed-over ADHS configuration.
- Resolution at send time from the project owning the course.
- Mailpit as the local target in development.

**Acceptance criteria**

- [ ] Credentials are write-only, encrypted at rest, and never returned by any
      endpoint
- [ ] Mail is sent using the credentials of the project that owns the course, not
      a global default
- [ ] A project with no SMTP configuration fails loudly at configuration time,
      not silently at send time
- [ ] Development sends to Mailpit and can never reach a real recipient

---

### P8-03 · Delivery and retry

- **jira:** _(unassigned)_
- **Estimate:** 1.5 h · **Week:** 5 · **Depends on:** P8-01, P8-02
- **Review gate:** human — certificates

**Scope**

- Send the certificate to the learner on completion, with the PDF attached.
- Durable retry with backoff; distinguish permanent rejection (invalid address)
  from transient failure.
- Delivery status persisted and visible in the P9 participant list.

**Acceptance criteria**

- [x] A transient SMTP failure is retried; a hard bounce is not retried forever
- [x] Retries survive an API restart — the queue is a database table, not memory
- [x] A failed delivery never blocks the certificate being downloadable (P8-04)
- [x] Delivery status is queryable per participant (`certificateState`)
- [x] The email body contains no EFN — asserted by test, alongside no score
- [x] An erased subject is not pursued: the address is read live from `users`,
      so ADR-0008's nulling stops delivery without erasure knowing this queue
      exists

**Built as** a `deliveryChannel` plugin (`@ds/mail`, ADR-0010) plus a worker
mirroring the EIV sweep: `claim_due_certificate_deliveries` (migration 0012)
leases work across tenants without weakening RLS, and `planDeliveryAttempt` in
`@ds/domain` decides the backoff — 1 min, 5 min, 25 min, 2 h, 6 h, then stop.

**Deliberately unlike the EIV queue in two ways.** It backs off over a day
rather than retrying hard, because a certificate has no statutory deadline and
the failures it sees (greylisting, quota, a restarting server) are fixed by
waiting. And no outcome raises an alert: the learner can already download the
certificate, so paging somebody at the weekend would be crying wolf against the
EIV alerts that matter.

---

### P8-04 · Certificate download endpoint

- **jira:** _(unassigned)_
- **Estimate:** 1.5 h · **Week:** 5 · **Depends on:** P8-03
- **Review gate:** human — certificates

**Context**
Email delivery fails for reasons outside our control — full mailboxes, corporate
filters. Download is the path that always works, which is why P8-03 explicitly
does not gate it.

**Scope**

- Authenticated download scoped to the owning learner.
- Tenant-scoped; admins download within their own scope only.

**Acceptance criteria**

- [ ] A learner can download only their own certificate
- [ ] A certificate URL is not guessable and does not expose an enumerable
      identifier
- [ ] Download works even when email delivery has permanently failed
- [ ] A request for another learner's certificate returns 404, not 403 —
      existence is not disclosed

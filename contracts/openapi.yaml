# The API contract (P0-06 / P2-06). Written before implementation for new
# surface, and the single source the SDK generates from — CI fails if
# packages/sdk/src/generated drifts from this file.
#
# Scope: exactly what the API implements. Endpoints land here first, then in
# code; nothing lands here speculatively. The learner-progress, quiz,
# evaluation and certificate paths arrive with their phases (P3–P8).
#
# Conventions (CLAUDE.md §5):
# - Errors are RFC 7807 problem documents, application/problem+json.
# - Percentages are integers 0–100. Durations are integer seconds.
# - Timestamps are ISO 8601 UTC. German local time is presentation only.
# - 404 for "not visible in this tenant" — existence is never disclosed.

openapi: 3.1.0

info:
  title: DS Education Platform API
  version: 0.1.0
  description: |
    Headless learner API for the DS Education Platform (CME).

    Every request carries two credentials:
    - a **bearer token**, validated server-side against the project's own
      Keycloak realm (signature, issuer, audience, expiry — ADR-0003), and
    - the **X-DS-Project header**, identifying which host surface is calling
      (ADR-0007). The project binding decides the Keycloak realm and pins the
      tenant; a token from another realm is rejected regardless of validity.

    All compliance decisions are server-side. The client renders; it is never
    the source of truth for anything affecting a CME point.

servers:
  - url: /
    description: Relative to wherever the API is deployed.

security:
  - bearerAuth: []

tags:
  - name: health
  - name: catalog
  - name: learning
  - name: assessment
  - name: completion
  - name: certificate
  - name: admin

paths:
  /health:
    get:
      tags: [health]
      operationId: getHealth
      summary: Liveness/readiness probe
      description: Public — a load balancer cannot present a bearer token.
      security: []
      responses:
        "200":
          description: The API and its database are reachable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  /branding:
    get:
      tags: [catalog]
      operationId: getBranding
      summary: White-label branding for a project
      description: |
        Logo, colours and typeface for the project named in `X-DS-Project`.

        **Public**, and one of only two routes that are. The widget renders
        branded loading, session-expired and misconfiguration states before it
        holds a token, and the admin console's login screen never holds one.

        What it discloses is what the customer's public website already shows
        every visitor. It returns no id, no Keycloak binding and no SMTP
        settings even though those live on the same row — enforced in SQL by a
        narrow SECURITY DEFINER function, not by this handler.

        An unknown slug returns **empty branding with 200**, identical to a
        known-but-unbranded project: a 404 here would be a project-slug oracle
        (ADR-0007).

        `fontFamily` is a CSS font stack, never a URL. The platform loads no
        third-party font — a German healthcare site transmitting visitor IPs to
        a font CDN is a GDPR exposure, not a convenience. When the customer has
        uploaded their own typeface, `fontFamilyName` and `fontVersion` name it
        and the file itself comes from `GET /branding/font` on this same origin.
      security: []
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
      responses:
        "200":
          description: The branding, possibly empty.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Branding" }

  /branding/font:
    get:
      tags: [catalog]
      operationId: getBrandingFont
      summary: The project's uploaded webfont
      description: |
        The font file for the project named in `X-DS-Project`, as uploaded by a
        customer admin.

        **Public, and that is the GDPR position.** Serving the typeface from
        this origin means a learner's browser never contacts a font CDN, no IP
        address reaches a third party, and there is nothing here for a consent
        banner or a processing record to cover.

        `woff2` or `woff` only, and which one is decided by the file's own
        container signature rather than by anything the uploader declared. The
        response is immutable and cached for a year; callers append
        `?v=<fontVersion>` from `GET /branding`, so replacing a font changes
        the URL rather than needing a purge.

        A project with no font — or no such project — returns 404. A font is
        evidence of nothing, so this one does not need ADR-0007's silence.
      security: []
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - name: project
          in: query
          required: false
          description: |
            The project slug, for callers that cannot set a header. This is the
            one route a **browser** requests on its own behalf — from an
            `@font-face` rule, where the CSS engine sends no custom headers and
            there is no hook to add one. The header wins when both are given.
          schema: { type: string }
        - name: v
          in: query
          required: false
          description: Cache-busting token; ignored by the server.
          schema: { type: string }
      responses:
        "200":
          description: The font file.
          content:
            font/woff2:
              schema: { type: string, format: binary }
            font/woff:
              schema: { type: string, format: binary }
        "404": { $ref: "#/components/responses/NotFound" }

  /courses:
    get:
      tags: [catalog]
      operationId: listCourses
      summary: List the tenant's courses
      description: |
        Backs the course list screen: the delivery-type tabs, the Thema and
        Altersgruppe filters with their facet counts, pagination, and the card
        metadata line ("5 CME Punkte | 5 Module | 2 Stunden 30 Minuten").
        Rows are scoped to the caller's tenant by PostgreSQL RLS (ADR-0002).
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - name: thema
          in: query
          schema: { type: string }
        - name: altersgruppe
          in: query
          schema: { type: string }
        - name: deliveryType
          in: query
          description: |
            A comma-separated **set** of delivery types, e.g. `live,praesenz`.

            The catalogue's tabs are functions rather than delivery types — the
            layout draws `On Demand` and `Weitere` — so one tab has to be able
            to name several. Comma-separated in a single parameter rather than
            the parameter repeated, because repeated query parameters have
            several incompatible serialisations and none of these values can
            contain a comma.

            Omitted means every delivery type.
          schema:
            type: string
            pattern: "^(on_demand|live|praesenz)(,(on_demand|live|praesenz))*$"
          example: "live,praesenz"
        - name: page
          in: query
          schema: { type: integer, minimum: 1, default: 1 }
        - name: perPage
          in: query
          schema: { type: integer, minimum: 1, maximum: 50, default: 10 }
      responses:
        "200":
          description: One page of courses plus facet counts.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CourseListResponse"
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "422": { $ref: "#/components/responses/ValidationFailed" }

  /courses/{slug}:
    get:
      tags: [catalog]
      operationId: getCourseBySlug
      summary: One course with its full module tree
      description: |
        The whole tree in one response — modules, chapters, contents, experts —
        so the detail view does not waterfall. Quiz contents carry no options
        and no answers here; the assessment endpoints (P4) own those, and the
        answer key never appears in any learner-facing shape.
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - name: slug
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: The course detail tree.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CourseDetail"
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /courses/{slug}/enrolment:
    put:
      tags: [learning]
      operationId: enrol
      summary: Start (or resume) this course
      description: |
        Idempotent: PUT again and the existing enrolment comes back unchanged.
        On first call the course's compliance settings — required watch
        percentage, pass threshold, CME points, VNR — are **snapshotted onto
        the enrolment**, so a later edit to the course record cannot
        retroactively invalidate work already done (P3-01).
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - $ref: "#/components/parameters/CourseSlug"
      responses:
        "200":
          description: The enrolment state, existing or newly created.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnrolmentState" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

    get:
      tags: [learning]
      operationId: getEnrolment
      summary: Progress, gating and resume point
      description: |
        The single resource the player screen renders from: the "Ihr
        Fortschritt — X von Y" ring, each module's and chapter's lock state,
        the resume target for "Fortbildung fortsetzen", and the outstanding
        conditions still standing between the learner and their CME points.

        Every value here is computed server-side from stored progress
        (`@ds/domain`); the client never supplies a percentage or a lock state.
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - $ref: "#/components/parameters/CourseSlug"
      responses:
        "200":
          description: The current enrolment state.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnrolmentState" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: No enrolment yet, or the course is not visible.
          content:
            application/problem+json:
              schema: { $ref: "#/components/schemas/ProblemDetails" }

  /courses/{slug}/contents/{contentId}:
    get:
      tags: [learning]
      operationId: getLesson
      summary: Open a lesson
      description: |
        The lesson payload — the video URL or the text body — served **only
        after the sequence gate agrees**.

        This is why `CourseDetail` carries no `videoUrl`: a browse response
        listing every video URL in the course would make the padlock on a later
        chapter decorative, since the URL would already be readable while the
        first chapter is unfinished. Here the gate is checked before anything
        is produced, by the same `requireReachableContent` the progress and
        quiz endpoints use.

        A quiz is not openable here — it has its own endpoint whose response
        shape cannot carry a correct answer.
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - $ref: "#/components/parameters/CourseSlug"
        - $ref: "#/components/parameters/ContentId"
      responses:
        "200":
          description: The lesson.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/LessonContent" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403":
          description: |
            The sequence gate refuses: an earlier chapter is still outstanding.
          content:
            application/problem+json:
              schema: { $ref: "#/components/schemas/ProblemDetails" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422": { $ref: "#/components/responses/ValidationFailed" }

  /courses/{slug}/contents/{contentId}/progress:
    post:
      tags: [learning]
      operationId: recordProgress
      summary: Report watched intervals for a video
      description: |
        The client reports **which intervals it played**, never a percentage.
        The server merges them into the stored union and recomputes coverage
        (`CLAUDE.md` §4 invariant 5) — a maximum-position implementation would
        make the gate skippable by dragging the scrub bar.

        Segments claiming more playback than wall-clock time elapsed since the
        last report are rejected and named in `rejected`, so tampering is
        visible rather than silently discarded. The response always carries
        the server's own recomputed figures.
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - $ref: "#/components/parameters/CourseSlug"
        - $ref: "#/components/parameters/ContentId"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ProgressReport" }
      responses:
        "200":
          description: Progress after merging the accepted intervals.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ProgressResult" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403":
          description: |
            The content is locked — everything before it is not yet complete.
          content:
            application/problem+json:
              schema: { $ref: "#/components/schemas/ProblemDetails" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422": { $ref: "#/components/responses/ValidationFailed" }

  /courses/{slug}/contents/{contentId}/quiz:
    get:
      tags: [assessment]
      operationId: getQuiz
      summary: The Lernerfolgskontrolle questions
      description: |
        Questions and options with **no correctness marker of any kind** — the
        shape has nowhere to put one (P4-01). Scoring happens exclusively
        server-side.
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - $ref: "#/components/parameters/CourseSlug"
        - $ref: "#/components/parameters/ContentId"
      responses:
        "200":
          description: The quiz, without answers.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Quiz" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403":
          description: Locked until the preceding content is complete.
          content:
            application/problem+json:
              schema: { $ref: "#/components/schemas/ProblemDetails" }
        "404": { $ref: "#/components/responses/NotFound" }

    post:
      tags: [assessment]
      operationId: submitQuiz
      summary: Submit an attempt
      description: |
        Scored server-side against the enrolment's snapshotted pass threshold.
        The response reports the score and pass/fail; per-question correctness
        is recorded in the attempt but only returned when the course sets
        `revealCorrectAnswers`, which a CME-certified course never does.
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - $ref: "#/components/parameters/CourseSlug"
        - $ref: "#/components/parameters/ContentId"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/QuizSubmission" }
      responses:
        "200":
          description: The scored attempt.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/QuizAttemptResult" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403":
          description: Locked, or the attempt limit is exhausted.
          content:
            application/problem+json:
              schema: { $ref: "#/components/schemas/ProblemDetails" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422": { $ref: "#/components/responses/ValidationFailed" }

  /courses/{slug}/materials:
    get:
      tags: [learning]
      operationId: getMaterials
      summary: The Mediathek — downloadable material, grouped by module
      description: |
        Backs the Mediathek tab. Material belonging to a module the learner has
        not finished is returned **locked and without its download URL** — the
        padlocked section in the layout, with "Wird nach Abschluss der Module
        freigeschaltet".

        Withholding the URL rather than only a flag is the point: a `locked:
        true` the client is trusted to honour is not a gate, it is a
        suggestion.
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - $ref: "#/components/parameters/CourseSlug"
      responses:
        "200":
          description: Material grouped by module, with lock state.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MaterialLibrary" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "404": { $ref: "#/components/responses/NotFound" }

  /courses/{slug}/evaluation:
    get:
      tags: [completion]
      operationId: getEvaluation
      summary: The evaluation questionnaire
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - $ref: "#/components/parameters/CourseSlug"
      responses:
        "200":
          description: The questionnaire and whether it is already submitted.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Evaluation" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "404": { $ref: "#/components/responses/NotFound" }

    post:
      tags: [completion]
      operationId: submitEvaluation
      summary: Submit the evaluation
      description: |
        Required for completion (the Anerkennungsbescheid makes it a condition
        of the Punktemeldung). Free-text answers are personal data and never
        reach a log (ADR-0004).
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - $ref: "#/components/parameters/CourseSlug"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EvaluationSubmission" }
      responses:
        "200":
          description: Enrolment state after recording the evaluation.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnrolmentState" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: Already submitted.
          content:
            application/problem+json:
              schema: { $ref: "#/components/schemas/ProblemDetails" }
        "422": { $ref: "#/components/responses/ValidationFailed" }

  /courses/{slug}/certificate:
    get:
      tags: [certificate]
      operationId: getCertificate
      summary: The certificate's fields
      description: |
        The assembled Teilnahmebescheinigung data, for rendering a preview
        before download. Available only once the course is complete.
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - $ref: "#/components/parameters/CourseSlug"
      responses:
        "200":
          description: The certificate data.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Certificate" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "404":
          description: Not enrolled, or the course is not complete.
          content:
            application/problem+json:
              schema: { $ref: "#/components/schemas/ProblemDetails" }

  /courses/{slug}/certificate/pdf:
    get:
      tags: [certificate]
      operationId: downloadCertificate
      summary: Download the Teilnahmebescheinigung as a PDF
      description: |
        Renders the ÄKWL Muster: participant, course, both VNR barcodes
        (Code 39 and Datamatrix), the participation date, the creditability
        sentence, and the stamp and signature of the course's Wissenschaftliche
        Leitung.

        Refuses with 409 if the course has no stamp or signature configured —
        the Bescheid makes a certificate without them invalid, and issuing an
        invalid-looking document to a physician who has earned their points is
        worse than telling the admin their course is incomplete.
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - $ref: "#/components/parameters/CourseSlug"
      responses:
        "200":
          description: The certificate.
          content:
            application/pdf:
              schema: { type: string, format: binary }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: |
            Not complete, or the course is missing its signing assets.
          content:
            application/problem+json:
              schema: { $ref: "#/components/schemas/ProblemDetails" }

  /admin/courses:
    get:
      tags: [admin]
      operationId: adminListCourses
      summary: The tenant's courses, with their authoring status
      description: |
        Scoped by PostgreSQL RLS to the caller's customer (ADR-0002), like
        everything else. `certificateReady` and `missingCertificateFields` are
        computed with the **same rule the certificate endpoint enforces**, so
        the console cannot report a course as ready that would then refuse to
        issue.
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
      responses:
        "200":
          description: The courses this admin may see.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/AdminCourseSummary" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      tags: [admin]
      operationId: adminCreateCourse
      summary: Create a course
      description: |
        Deliberately minimal. Everything a certificate needs — VNR, points,
        Veranstalter, the Wissenschaftliche Leitung — is set afterwards on the
        settings screen, which already refuses a pass threshold below the
        accredited minimum and reports which certificate fields are missing.
        Duplicating those rules into a creation form would be a second place to
        get them wrong.
      parameters: [{ $ref: "#/components/parameters/ProjectHeader" }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CourseCreate" }
      responses:
        "201":
          description: The new course's (empty) structure.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CourseStructure" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
        "422": { $ref: "#/components/responses/ValidationFailed" }

  /admin/courses/{slug}:
    get:
      tags: [admin]
      operationId: adminGetCourse
      summary: One course's settings
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - $ref: "#/components/parameters/CourseSlug"
      responses:
        "200":
          description: The course.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminCourseDetail" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

    patch:
      tags: [admin]
      operationId: adminUpdateCourse
      summary: Edit a course's compliance and certificate fields
      description: |
        Every field is optional — an absent field means "leave alone", so
        editing the issue place does not require resending the VNR password.

        **Lowering `passThresholdPercent` below the accredited minimum is
        refused** unless `acknowledgeAccreditationRisk` is true. The 70 %
        threshold is a condition of the Anerkennungsbescheid, not a difficulty
        setting: below it the Ärztekammer does not credit the points, so every
        point awarded afterwards is unearned. The check is server-side because
        a confirmation a client can skip is not a confirmation.

        Changes never apply retroactively — an enrolment carries the thresholds
        snapshotted when it was created (P3-01).
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - $ref: "#/components/parameters/CourseSlug"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AdminCourseUpdate" }
      responses:
        "200":
          description: The updated course.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminCourseDetail" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: |
            The pass threshold would fall below the accredited minimum and the
            risk was not acknowledged.
          content:
            application/problem+json:
              schema: { $ref: "#/components/schemas/ProblemDetails" }
        "422": { $ref: "#/components/responses/ValidationFailed" }

    delete:
      tags: [admin]
      operationId: adminDeleteCourse
      summary: Delete an empty course
      description: |
        Refused with 409 while it still contains modules, and permanently
        refused once anybody has enrolled — an enrolment is the record behind a
        CME point that may already have been reported to an Ärztekammer, and
        removing it would leave the point credited with nothing to show what
        earned it.

        Enrolments and not watch progress: a learner who enrolled and never
        pressed play has no progress rows and still has a record worth keeping.
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - $ref: "#/components/parameters/CourseSlug"
      responses:
        "204": { description: Deleted. }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /admin/courses/{slug}/certificate-assets:
    put:
      tags: [admin]
      operationId: adminSetCertificateAssets
      summary: Upload the stamp and signature of the Wissenschaftliche Leitung
      description: |
        The Bescheid requires the Teilnahmebescheinigung to carry the stamp of,
        and be signed by, the Wissenschaftliche Leitung — and those belong to
        the **course**, supplied by whoever creates it, not to the platform.

        PUT because it replaces: a certificate carries one stamp, so "add
        another" has no meaning. Base64 in JSON rather than multipart — these
        are a few kilobytes, and a multipart parser would add a file-upload
        surface for one endpoint.

        PNG and JPEG only, 512 KB each, and the **magic bytes are checked
        rather than the declared type**. SVG is deliberately excluded: it is
        executable markup, and this asset is customer-uploaded and rendered by
        the server into a PDF.
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - $ref: "#/components/parameters/CourseSlug"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CertificateAssetUpload" }
      responses:
        "200":
          description: The updated course.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminCourseDetail" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422": { $ref: "#/components/responses/ValidationFailed" }

  /admin/branding/font:
    get:
      tags: [admin]
      operationId: adminGetFont
      summary: What font the project has on file
      description: |
        Metadata only: the family name, the version, and the size in bytes.
        Never the file — the console previews it by pointing an `@font-face` at
        `GET /branding/font`, exactly as a learner's browser does. A second code
        path to the same bytes would mean the one that matters is the one
        nobody tested.
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
      responses:
        "200":
          description: The stored font, or nulls if there is none.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FontState" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    put:
      tags: [admin]
      operationId: adminSetFont
      summary: Upload the project's webfont
      description: |
        White-labelling: the platform is sold to more than one customer, so the
        typeface is data on the project, not a constant in a stylesheet
        (P10-08).

        `woff2` or `woff`, up to 2 MB, base64 in JSON. **The bytes decide the
        format** — the container signature is read and the file must be exactly
        as long as its own header claims, which rejects a font with a payload
        appended to it. The declared `fontMime` is only cross-checked against
        that; it can permit nothing.

        **SVG fonts are impossible.** An SVG font is executable markup, and this
        file is served from the API's own origin to a page holding a bearer
        token.

        `fontFamilyName` is emitted inside an `@font-face` block, so it is
        restricted to letters, digits, spaces, hyphen and underscore in
        `@ds/domain`, in this schema, and in a database CHECK.

        Which project is written comes from `X-DS-Project`, and the write runs
        under RLS: a slug belonging to another customer updates nothing and
        returns 404.
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/FontUpload" }
      responses:
        "200":
          description: The stored font.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FontState" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422": { $ref: "#/components/responses/ValidationFailed" }
    delete:
      tags: [admin]
      operationId: adminClearFont
      summary: Remove the project's webfont
      description: |
        Learners fall back to the configured `fontFamily` stack, which is always
        valid — a project without an uploaded font is the normal state, not a
        degraded one.
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
      responses:
        "200":
          description: The now-empty font state.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/FontState" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # ---------------------------------------------------------------------------
  # Authoring (P9-02, P9-04, P9-05)
  #
  # Every mutating route returns the whole structure it changed. An authoring
  # screen re-renders after each edit and the edit is rarely local — creating a
  # module appends it, deleting one renumbers what follows, a reorder moves
  # several things at once. Returning the tree makes the client's state the
  # server's answer rather than a local edit that has to agree with it.
  # ---------------------------------------------------------------------------

  /admin/learners:
    get:
      tags: [admin]
      operationId: adminListLearners
      summary: Every enrolment in this tenant, with its progress
      description: |
        The per-learner progress view. Carries a **masked** EFN — the last four
        digits — and never the whole value: an operator confirming they have the
        right person needs to recognise it, not to read it (ADR-0004).
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - name: course
          in: query
          required: false
          description: Restrict to one course's slug. Absent means all courses.
          schema: { type: string }
      responses:
        "200":
          description: The enrolments.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/LearnerRecord" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /admin/learners/{enrolmentId}/name:
    patch:
      tags: [admin]
      operationId: adminCorrectLearnerName
      summary: Correct the name a certificate will carry
      description: |
        Allowed until the Punktemeldung is accepted, and refused with 409 after
        it — the name is on the Ärztekammer's record by then, and a silent edit
        would make the two disagree. The correction path after that point is a
        written one inside the seven-day window.
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - name: enrolmentId
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name: { type: string, minLength: 1, maxLength: 300 }
      responses:
        "204": { description: Corrected. }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
        "422": { $ref: "#/components/responses/ValidationFailed" }

  /admin/learners/{enrolmentId}:
    delete:
      tags: [admin]
      operationId: adminEraseSubject
      summary: Erase a subject (GDPR Art. 17)
      description: |
        Performed by `erase_subject`, which pseudonymises rather than deleting
        so the CME record survives, refuses while a Punktemeldung is open, and
        writes its own audit row. Irreversible and cross-tenant — a physician
        may hold enrolments at several customers and all of them are erased.
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - name: enrolmentId
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [reason]
              properties:
                reason:
                  type: string
                  minLength: 1
                  maxLength: 200
                  description: |
                    For the audit trail. About the *process* — "Löschantrag vom
                    12.03." — never about the person.
      responses:
        "200":
          description: What was erased.
          content:
            application/json:
              schema:
                type: object
                required: [enrolments, responses, submissions]
                properties:
                  enrolments: { type: integer, minimum: 0 }
                  responses: { type: integer, minimum: 0 }
                  submissions: { type: integer, minimum: 0 }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /admin/certificates:
    get:
      tags: [admin]
      operationId: adminListCertificates
      summary: Certificates in this tenant
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - name: course
          in: query
          required: false
          schema: { type: string }
      responses:
        "200":
          description: The certificates.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/CertificateRecord" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /admin/certificates/{id}/regenerate:
    post:
      tags: [admin]
      operationId: adminRegenerateCertificate
      summary: Re-render a certificate
      description: |
        Re-renders from the enrolment, picking up a corrected name. **Reports
        nothing to EIV** — the certificate and submission pipelines share no
        code path, so a regeneration cannot trigger one.
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - $ref: "#/components/parameters/CertificateId"
      responses:
        "204": { description: Queued for re-rendering. }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /admin/certificates/{id}/resend:
    post:
      tags: [admin]
      operationId: adminResendCertificate
      summary: Put an issued certificate back in the delivery queue
      description: The same document, sent again. Not a reissue.
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - $ref: "#/components/parameters/CertificateId"
      responses:
        "204": { description: Queued for delivery. }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /admin/certificates/{id}/revoke:
    post:
      tags: [admin]
      operationId: adminRevokeCertificate
      summary: Withdraw a certificate
      description: |
        Withdraws the document and keeps the record. The enrolment, the
        progress and any Punktemeldung stay exactly where they were — what the
        physician earned, they earned.
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - $ref: "#/components/parameters/CertificateId"
      responses:
        "204": { description: Revoked. }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /admin/staff:
    get:
      tags: [admin]
      operationId: adminListStaff
      summary: Operator accounts this caller may manage
      description: |
        Above any tenant — no `X-DS-Project` — and narrowed by `canGrant`: an
        operator who may not grant to a scope has no business reading the
        accounts in it, so a customer administrator does not see the super
        administrators above them.
      responses:
        "200":
          description: The accounts.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/StaffAccount" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      tags: [admin]
      operationId: adminInviteStaff
      summary: Invite an operator
      description: |
        Creates the account with **no password** and returns a single-use token.
        The token is returned rather than emailed; the operator passes the link
        on. Until it is redeemed the account cannot sign in, so an un-redeemed
        invitation is not a credential.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/StaffInvitation" }
      responses:
        "201":
          description: The invitation.
          content:
            application/json:
              schema:
                type: object
                required: [status, token]
                properties:
                  status: { type: string }
                  token: { type: string }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "422": { $ref: "#/components/responses/ValidationFailed" }

  /admin/staff/{id}/scope:
    post:
      tags: [admin]
      operationId: adminSetStaffScope
      summary: Change what an account may reach
      parameters: [{ $ref: "#/components/parameters/StaffId" }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/StaffScope" }
      responses:
        "204": { description: Changed. }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /admin/staff/{id}/disabled:
    post:
      tags: [admin]
      operationId: adminSetStaffDisabled
      summary: Disable or re-enable an account
      description: Disabling revokes every session in the same statement.
      parameters: [{ $ref: "#/components/parameters/StaffId" }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [disabled]
              properties:
                disabled: { type: boolean }
      responses:
        "204": { description: Changed. }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /admin/staff/{id}/sign-out-everywhere:
    post:
      tags: [admin]
      operationId: adminSignOutStaffEverywhere
      summary: Revoke every session an account holds
      parameters: [{ $ref: "#/components/parameters/StaffId" }]
      responses:
        "204": { description: Signed out. }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /admin/staff/{id}/second-factor/reset:
    post:
      tags: [admin]
      operationId: adminResetStaffSecondFactor
      summary: Clear an operator's second factor so they can enrol a new device
      description: |
        The lost-device path (P22-02). Before it existed, an enrolled operator
        whose authenticator was gone was locked out permanently — there was no
        reset anywhere in the product, and for a `super_admin`, the one role
        that was forced to enrol, a lost phone could end the platform's only
        unrestricted account.

        It does **not** sign the target in and does **not** relax their policy:
        under a `required` policy their next sign-in goes to enrolment, so
        access is restored without the bar moving. Every session they hold is
        revoked at the same time.

        Same authorization as re-scoping the account, plus one rule: **nobody
        may reset their own**. Self-reset would turn a stolen session into a
        permanently weakened account, and would step around the `required`
        policy that `DELETE /admin/auth/second-factor` enforces.
      parameters: [{ $ref: "#/components/parameters/StaffId" }]
      responses:
        "204":
          {
            description: Cleared. The account must enrol again if its policy requires one.,
          }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /admin/auth/second-factor/policy:
    get:
      tags: [admin]
      operationId: adminGetSecondFactorPolicy
      summary: The second-factor policies in force
      description: |
        Every operator may read the rules they are subject to. Which of them
        they may change is enforced on the `PUT`, not by hiding this — a hidden
        control is a convenience and the refusal is the boundary.
      responses:
        "200":
          description: The platform policy, and one entry per customer that has set its own.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/SecondFactorPolicies" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
    put:
      tags: [admin]
      operationId: adminSetSecondFactorPolicy
      summary: Turn the second factor off, make it optional, or make it mandatory
      description: |
        Three values, and they are genuinely three (P22-02):

        - `disabled` — not offered, and an already-enrolled secret is not asked
          for. This is what lets an operator whose device is gone back in.
        - `optional` — enrol if you want to; **if you have, you must use it**.
          Relaxing a policy must never make a stolen password sufficient for
          somebody who had already protected themselves.
        - `required` — everybody enrols, and an account that has not is sent to
          enrolment rather than refused, which would make it unrecoverable.

        `customerId: null` names the platform itself — the scope a `super_admin`
        belongs to, since they belong to no customer — and **only a
        `super_admin` may set it**. Anyone else would be deciding, from inside
        one customer, how strictly the platform's unrestricted accounts are
        protected. A customer administrator may set their own customer's policy
        and no other.

        The platform default is `required`, which is ADR-0012's original rule
        kept as a default rather than a law. Every change is written to
        `admin_audit_log` carrying `weakened: true|false`, because "somebody
        relaxed a security policy" is the fact an auditor scans for.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SecondFactorPolicyUpdate" }
      responses:
        "200":
          description: Saved.
          content:
            application/json:
              schema:
                type: object
                required: [status]
                properties: { status: { type: string } }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /admin/auth/second-factor:
    delete:
      tags: [admin]
      operationId: adminRemoveOwnSecondFactor
      summary: Take your own second factor off
      description: |
        Refused with 403 when the policy governing your account is `required` —
        which is what makes "mandatory" mean something rather than being a
        suggestion. Under `optional` it is your own call, and under `disabled`
        the factor is not being used anyway.

        Clearing **somebody else's** is a different operation with different
        rules: `POST /admin/staff/{id}/second-factor/reset`.
      responses:
        "200":
          description: Removed.
          content:
            application/json:
              schema:
                type: object
                required: [status]
                properties: { status: { type: string } }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /admin/customers:
    get:
      tags: [admin]
      operationId: adminListCustomers
      summary: Every customer on the platform
      description: |
        Above any tenant, and therefore authenticated by a **staff session
        cookie** rather than a learner bearer token — there is no
        `X-DS-Project` header on these requests because the list spans
        projects (ADR-0012).

        Requires the `customer` capability, which only `super_admin` holds: a
        customer is the tenant boundary itself, so nobody inside one may see or
        mint another. A customer administrator receives 403.
      responses:
        "200":
          description: The customers, by name.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/CustomerSummary" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      tags: [admin]
      operationId: adminCreateCustomer
      summary: Create a customer
      description: |
        The API generates the id and opens an RLS tenant context on it before
        inserting, so the new row satisfies the `customers` policy rather than
        bypassing it (ADR-0002).
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CustomerCreate" }
      responses:
        "201":
          description: The new customer.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CustomerSummary" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
        "422": { $ref: "#/components/responses/ValidationFailed" }
        "429":
          description: |
            Too many customers created too quickly. A customer is a tenant
            boundary; nobody creates them in bulk, and an accidental loop in a
            console script should stop rather than seed empty tenants.
          content:
            application/problem+json:
              schema: { $ref: "#/components/schemas/ProblemDetails" }

  /admin/customers/{slug}:
    parameters:
      - name: slug
        in: path
        required: true
        description: The customer's slug.
        schema: { type: string }
    get:
      tags: [admin]
      operationId: adminGetCustomer
      summary: One customer
      responses:
        "200":
          description: The customer.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CustomerSummary" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      tags: [admin]
      operationId: adminUpdateCustomer
      summary: Rename a customer
      description: |
        The slug cannot be changed here. It is what links, bookmarks and
        runbooks refer to, and re-slugging through the same call that fixes a
        typo in a company name breaks them with nothing in the audit trail to
        explain it.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CustomerUpdate" }
      responses:
        "200":
          description: The renamed customer.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CustomerSummary" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422": { $ref: "#/components/responses/ValidationFailed" }
    delete:
      tags: [admin]
      operationId: adminDeleteCustomer
      summary: Delete an empty customer
      description: |
        Refused with 409 while anything is inside it, and permanently refused
        once any learner record exists anywhere beneath it. The `detail` names
        the counts, so the refusal is an instruction rather than a dead end.
        Never a cascade.
      responses:
        "204": { description: Deleted. }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /admin/departments:
    get:
      tags: [admin]
      operationId: adminListDepartments
      summary: Departments in this tenant
      parameters: [{ $ref: "#/components/parameters/ProjectHeader" }]
      responses:
        "200":
          description: The departments.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/DepartmentSummary" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      tags: [admin]
      operationId: adminCreateDepartment
      summary: Create a department
      description: |
        The insert names no customer: `customer_id` comes from the RLS session
        variable, so a department cannot be created into another tenant because
        there is no way to say which tenant (ADR-0002).
      parameters: [{ $ref: "#/components/parameters/ProjectHeader" }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/DepartmentCreate" }
      responses:
        "201":
          description: The departments, including the new one.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/DepartmentSummary" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
        "422": { $ref: "#/components/responses/ValidationFailed" }

  /admin/departments/{slug}:
    patch:
      tags: [admin]
      operationId: adminUpdateDepartment
      summary: Rename a department
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - name: slug
          in: path
          required: true
          description: The department's slug.
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/DepartmentUpdate" }
      responses:
        "200":
          description: The departments.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/DepartmentSummary" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      tags: [admin]
      operationId: adminDeleteDepartment
      summary: Delete an empty department
      description: |
        Refused with 409 while it still contains projects or courses, and
        permanently refused once a learner has enrolled anywhere beneath it.
        The `detail` names the counts.
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - name: slug
          in: path
          required: true
          description: The department's slug.
          schema: { type: string }
      responses:
        "200":
          description: The remaining departments.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/DepartmentSummary" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /admin/projects:
    get:
      tags: [admin]
      operationId: adminListProjects
      summary: Projects in this tenant
      description: |
        Carries the Keycloak binding and the SMTP settings, and **never** the
        SMTP password — `hasSmtpPassword` is the only readable trace, exactly
        as `hasVnrPassword` is for a course (CLAUDE.md §4 invariant 7).
      parameters: [{ $ref: "#/components/parameters/ProjectHeader" }]
      responses:
        "200":
          description: The projects.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/ProjectSummary" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      tags: [admin]
      operationId: adminCreateProject
      summary: Create a project
      parameters: [{ $ref: "#/components/parameters/ProjectHeader" }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ProjectCreate" }
      responses:
        "201":
          description: The projects, including the new one.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/ProjectSummary" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
        "422": { $ref: "#/components/responses/ValidationFailed" }

  /admin/projects/{slug}:
    patch:
      tags: [admin]
      operationId: adminUpdateProject
      summary: Edit a project, its branding, its Keycloak binding and its SMTP
      description: |
        The Keycloak binding decides which realm every token for this project is
        validated against (ADR-0003). Changing it wrongly locks every learner
        out at once, which is why the console confirms before saving it — the
        API applies what it is given, because a platform that refused to let an
        admin change their own IdP would be a worse failure than the one it
        prevented.

        `branding` is re-validated against `@ds/domain`'s grammars and the
        *parsed* result is stored, so a value that fails is dropped here rather
        than stored and dropped again on every read.

        `smtpPassword` is write-only.
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - name: slug
          in: path
          required: true
          description: |
            The project's own slug — the resource being edited, which is not
            necessarily the project in `X-DS-Project`.
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ProjectUpdate" }
      responses:
        "200":
          description: The projects.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/ProjectSummary" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422": { $ref: "#/components/responses/ValidationFailed" }
    delete:
      tags: [admin]
      operationId: adminDeleteProject
      summary: Delete an empty project
      description: |
        Refused with 409 while it still contains courses, and permanently
        refused once a learner has enrolled in any of them. The `detail` names
        the counts.
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - name: slug
          in: path
          required: true
          description: The project being deleted, not the one in `X-DS-Project`.
          schema: { type: string }
      responses:
        "200":
          description: The remaining projects.
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/ProjectSummary" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /admin/courses/{slug}/structure:
    get:
      tags: [admin]
      operationId: adminGetStructure
      summary: The authoring tree
      description: |
        Modules, chapters and content in author order, plus two counts the
        console needs: `learnerRecords`, which disables deletion and says why,
        and `questionCount`, which is zero for a quiz nobody can pass yet.
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - $ref: "#/components/parameters/CourseSlug"
      responses:
        "200":
          description: The structure.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CourseStructure" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /admin/courses/{slug}/modules:
    post:
      tags: [admin]
      operationId: adminCreateModule
      summary: Append a module
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - $ref: "#/components/parameters/CourseSlug"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ModuleWrite" }
      responses:
        "201":
          description: The structure.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CourseStructure" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422": { $ref: "#/components/responses/ValidationFailed" }

  /admin/modules/{id}:
    patch:
      tags: [admin]
      operationId: adminUpdateModule
      summary: Edit a module
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ModuleWrite" }
      responses:
        "200":
          description: The structure.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CourseStructure" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      tags: [admin]
      operationId: adminDeleteModule
      summary: Delete a module
      description: |
        Refused with 409 if any learner record points into it. That row is the
        evidence behind a CME point that may already have been reported to an
        Ärztekammer.
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: The structure.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CourseStructure" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /admin/modules/{id}/chapters:
    post:
      tags: [admin]
      operationId: adminCreateChapter
      summary: Append a chapter
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ChapterWrite" }
      responses:
        "201":
          description: The structure.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CourseStructure" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422": { $ref: "#/components/responses/ValidationFailed" }

  /admin/chapters/{id}:
    patch:
      tags: [admin]
      operationId: adminUpdateChapter
      summary: Edit a chapter
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ChapterWrite" }
      responses:
        "200":
          description: The structure.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CourseStructure" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      tags: [admin]
      operationId: adminDeleteChapter
      summary: Delete a chapter
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: The structure.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CourseStructure" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /admin/chapters/{id}/contents:
    post:
      tags: [admin]
      operationId: adminCreateContent
      summary: Append a content item
      description: |
        A `video` **must** carry `durationSec`. The watch requirement is a
        percentage of a known length; with no length there is nothing to reach,
        and the item would be skippable while appearing to count toward a CME
        point. Refused with 422 naming the field, not with a database error.
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ContentWrite" }
      responses:
        "201":
          description: The structure.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CourseStructure" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422": { $ref: "#/components/responses/ValidationFailed" }

  /admin/contents/{id}:
    patch:
      tags: [admin]
      operationId: adminUpdateContent
      summary: Edit a content item
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ContentWrite" }
      responses:
        "200":
          description: The structure.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CourseStructure" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422": { $ref: "#/components/responses/ValidationFailed" }
    delete:
      tags: [admin]
      operationId: adminDeleteContent
      summary: Delete a content item
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: The structure.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CourseStructure" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /admin/courses/{slug}/structure/order:
    put:
      tags: [admin]
      operationId: adminReorderStructure
      summary: Reorder the whole tree, atomically
      description: |
        Ids only — this route moves things, it never edits them. A chapter
        listed under a different module **is** the move, which is why this
        cannot be a set of per-list requests: a chapter in flight between two
        modules would briefly belong to neither.

        Every level is validated as a **permutation** of what exists before
        anything is written, so a request that is wrong anywhere changes nothing
        anywhere. A list that lost an item is rejected rather than obeyed —
        obeying it would delete a chapter from a course learners are part-way
        through.

        Ordering decides gating (`gating.ts`), so this is a
        compliance-adjacent operation rather than a convenience.
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - $ref: "#/components/parameters/CourseSlug"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/StructureOrder" }
      responses:
        "200":
          description: The structure, in its new order.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CourseStructure" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422": { $ref: "#/components/responses/ValidationFailed" }

  /admin/courses/{slug}/experts:
    put:
      tags: [admin]
      operationId: adminReplaceExperts
      summary: Replace the Experten/Referenten list
      description: |
        Replaced wholesale rather than diffed: no learner state points at an
        expert, so unlike everything else here a full replace is safe — and it
        is what the screen does anyway.
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - $ref: "#/components/parameters/CourseSlug"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ExpertsWrite" }
      responses:
        "200":
          description: The structure.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CourseStructure" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "422": { $ref: "#/components/responses/ValidationFailed" }

  /admin/contents/{id}/quiz:
    get:
      tags: [admin]
      operationId: adminGetQuiz
      summary: The quiz, with its answer key
      description: |
        **The only route in the platform that returns `isCorrect`.** It is
        reachable by an author role and nothing else; the learner-facing `Quiz`
        has no field capable of carrying the flag, which is what makes leaking
        it a compile error rather than something a reviewer has to notice
        (P4-01).

        `answerCount` per question is what blocks deletion: an
        already-submitted attempt must keep meaning what it meant.
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - $ref: "#/components/parameters/ResourceId"
      responses:
        "200":
          description: The quiz.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AuthoringQuiz" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    put:
      tags: [admin]
      operationId: adminSetQuiz
      summary: Replace the quiz
      description: |
        Diffed, not wiped and rewritten. `id` present means "the existing row,
        changed"; absent means "new"; anything the server holds and this
        document does not name is a deletion — and a deletion of something a
        learner has answered is refused with the count.

        Two refusals no form should be trusted with: a question with **no**
        correct answer cannot be passed by anybody, and a `single` question with
        **two** correct answers cannot either, because scoring is exact-set.
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - $ref: "#/components/parameters/ResourceId"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/QuizWrite" }
      responses:
        "200":
          description: The quiz, as stored.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AuthoringQuiz" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
        "422": { $ref: "#/components/responses/ValidationFailed" }

  /admin/courses/{slug}/evaluation:
    get:
      tags: [admin]
      operationId: adminGetEvaluation
      summary: The Evaluationsbogen, for editing
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - $ref: "#/components/parameters/CourseSlug"
      responses:
        "200":
          description: The evaluation.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AuthoringEvaluation" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    put:
      tags: [admin]
      operationId: adminSetEvaluation
      summary: Replace the Evaluationsbogen
      description: |
        Same diffing rule as the quiz: a question somebody has answered cannot
        be deleted. The Anerkennung requires an evaluation, so an empty one is
        a course that cannot be completed.
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - $ref: "#/components/parameters/CourseSlug"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EvaluationWrite" }
      responses:
        "200":
          description: The evaluation, as stored.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AuthoringEvaluation" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
        "422": { $ref: "#/components/responses/ValidationFailed" }

  /admin/courses/{slug}/participants:
    get:
      tags: [admin]
      operationId: adminListParticipants
      summary: Participant status for one course
      description: |
        Every figure comes from the **same rollup the learner's own screen
        renders from** (CLAUDE.md §4 invariant 6). A list showing a physician
        96 % where their own screen shows 100 % would be two different answers
        to "did this person earn a CME point", one of which has already gone to
        the Ärztekammer.

        `eivState` distinguishes `needs_attention` from a generic failure on
        purpose: a submission unresolved after the fast retries will not fix
        itself, and the Bescheid's paper fallback is only open while the 8-day
        deadline has not passed (P9-06).

        The EFN is never returned — only whether one is on file (ADR-0004).
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - $ref: "#/components/parameters/CourseSlug"
      responses:
        "200":
          description: The participants.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ParticipantList" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /admin/courses/{slug}/participants.csv:
    get:
      tags: [admin]
      operationId: adminExportParticipants
      summary: The participant list as CSV
      description: |
        Exactly the rows the list returns — built from the same call, not a
        second query. UTF-8 with a BOM and a `sep=;` line so Excel in a German
        locale opens it correctly, and cells that could be read as spreadsheet
        formulas are neutralised. The export is audited: it is the point where
        personal data leaves the system's access controls.
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - $ref: "#/components/parameters/CourseSlug"
      responses:
        "200":
          description: The CSV.
          content:
            text/csv:
              schema: { type: string }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /profile/efn:
    put:
      tags: [completion]
      operationId: setEfn
      summary: Store the learner's EFN
      description: |
        The EFN (Einheitliche Fortbildungsnummer) is 15 digits and is the key
        the Ärztekammer credits points against. Stored once per user, not per
        enrolment (ADR-0004), and never written to a log.
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/EfnInput" }
      responses:
        "204":
          description: Stored.
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "422":
          description: Not a valid 15-digit EFN.
          content:
            application/problem+json:
              schema: { $ref: "#/components/schemas/ProblemDetails" }

  /courses/{slug}/completion:
    post:
      tags: [completion]
      operationId: completeCourse
      summary: Finalise the course and report the points
      description: |
        Re-checks every condition server-side — watch coverage, quiz pass,
        evaluation, EFN — and refuses if any is outstanding, naming which.
        On success it stamps completion and queues the EIV submission with its
        statutory deadlines (8 days to report, 7 to correct). Idempotent: a
        second call returns the same completed state without re-queueing.
      parameters:
        - $ref: "#/components/parameters/ProjectHeader"
        - $ref: "#/components/parameters/CourseSlug"
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CompletionInput" }
      responses:
        "200":
          description: Completed; the CME points are queued for reporting.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/EnrolmentState" }
        "401": { $ref: "#/components/responses/Unauthenticated" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: |
            Conditions are still outstanding; `detail` names which, and the
            enrolment is unchanged.
          content:
            application/problem+json:
              schema: { $ref: "#/components/schemas/ProblemDetails" }

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: |
        Keycloak access token. Verified against the realm bound to the
        X-DS-Project header — issuer and audience come from that binding,
        never from the token (ADR-0003).

  parameters:
    CourseSlug:
      name: slug
      in: path
      required: true
      schema: { type: string }

    ContentId:
      name: contentId
      in: path
      required: true
      schema: { type: string, format: uuid }

    ResourceId:
      name: id
      in: path
      required: true
      description: |
        The id of the module, chapter or content item being addressed. Always a
        uuid, and always resolved under RLS: an id belonging to another tenant
        is a 404, indistinguishable from one that does not exist.
      schema: { type: string, format: uuid }

    CertificateId:
      name: id
      in: path
      required: true
      description: The certificate's id.
      schema: { type: string, format: uuid }

    StaffId:
      name: id
      in: path
      required: true
      description: The operator account's id.
      schema: { type: string, format: uuid }

    ProjectHeader:
      name: X-DS-Project
      in: header
      required: true
      description: |
        Project slug identifying the calling host surface (ADR-0007). Pins the
        tenant, and on the learner plane also resolves the Keycloak realm to
        validate the bearer token against.

        **How a bad slug is answered depends on which plane asked**, because the
        two callers know different things already (P22-01):

        - *Learner plane* (bearer token): an unknown **or unbound** slug is a
          generic `401`, never a `404` — whether a project exists is not a fact
          an anonymous caller should be able to enumerate, and a project with no
          Keycloak binding cannot authenticate anybody in any case.
        - *Staff plane* (session cookie, ADR-0012): an unknown slug is a `404`
          carrying `detail`. The caller is already authenticated and the
          platform knows who they are, so naming what was not found is both
          honest and safe. A staff session needs no identity provider at all, so
          a project **without** a Keycloak binding resolves normally here —
          answering 401 for that locked operators out of every tenant-scoped
          console screen on a project the console itself had just created.
        - Either plane, **header absent**: `422` with `detail`. The header is
          required; omitting it is a malformed request, not a failed
          authentication, and answering 401 makes a console send the operator
          back to a login form they never left.

        A caller who is authenticated but holds no grant reaching the resolved
        customer gets `403` on both planes.
      schema: { type: string }

  responses:
    Unauthenticated:
      description: |
        Missing/invalid token, missing project header, or unknown project.
        Always the same generic document — the reason is logged server-side
        only.
      content:
        application/problem+json:
          schema: { $ref: "#/components/schemas/ProblemDetails" }
    Forbidden:
      description: Authenticated, but no grant reaches this tenant.
      content:
        application/problem+json:
          schema: { $ref: "#/components/schemas/ProblemDetails" }
    NotFound:
      description: |
        Not found — or not visible in this tenant; the two are
        indistinguishable by design.
      content:
        application/problem+json:
          schema: { $ref: "#/components/schemas/ProblemDetails" }
    ValidationFailed:
      description: One or more request parameters are invalid.
      content:
        application/problem+json:
          schema: { $ref: "#/components/schemas/ProblemDetails" }
    Conflict:
      description: |
        The request is well-formed but the current state refuses it — a slug
        already taken, or a delete of something a learner has already used. The
        second kind is not a validation failure: nothing about the request is
        wrong, and the same request would have succeeded yesterday.
      content:
        application/problem+json:
          schema: { $ref: "#/components/schemas/ProblemDetails" }

  schemas:
    SecondFactorPolicy:
      type: string
      enum: [disabled, optional, required]
      description: |
        `disabled` — not offered, and an already-enrolled secret is not asked
        for. `optional` — enrol if you want to, but if you have you must use it.
        `required` — everybody enrols. See `PUT
        /admin/auth/second-factor/policy` for why `optional` is deliberately not
        the same as off.

    SecondFactorPolicies:
      type: object
      required: [platform, customers]
      properties:
        platform:
          allOf: [{ $ref: "#/components/schemas/SecondFactorPolicy" }]
          description: |
            The policy for accounts belonging to no customer — which is every
            `super_admin`. Defaults to `required`.
        customers:
          type: array
          description: One entry per customer that has set its own; the rest use `optional`.
          items:
            type: object
            required: [customerId, policy]
            properties:
              customerId: { type: string, format: uuid }
              policy: { $ref: "#/components/schemas/SecondFactorPolicy" }

    SecondFactorPolicyUpdate:
      type: object
      required: [customerId, policy]
      properties:
        customerId:
          type: string
          format: uuid
          nullable: true
          description: |
            `null` names the platform itself. Spelled as an explicit null rather
            than an absent field, so "set the platform policy" cannot be what a
            malformed request accidentally means.
        policy: { $ref: "#/components/schemas/SecondFactorPolicy" }

    ProblemDetails:
      type: object
      description: RFC 7807. No stack traces, no internal identifiers.
      required: [type, title, status]
      properties:
        type: { type: string, format: uri }
        title: { type: string }
        status: { type: integer }
        detail: { type: string }
        instance: { type: string }

    HealthStatus:
      type: object
      required: [status]
      properties:
        status:
          type: string
          enum: [ok, degraded]

    DeliveryType:
      type: string
      description: The course list's three tabs.
      enum: [on_demand, live, praesenz]

    CourseSummary:
      type: object
      description: One card on the course list screen.
      required:
        - id
        - slug
        - title
        - description
        - heroImageUrl
        - deliveryType
        - thema
        - altersgruppe
        - cmePoints
        - cmeCategory
        - moduleCount
        - totalDurationSec
        - enrolment
      properties:
        id: { type: string, format: uuid }
        slug: { type: string }
        title: { type: string }
        description: { type: [string, "null"] }
        heroImageUrl:
          type: [string, "null"]
          description: Course artwork; the card and the detail hero share it.
        deliveryType: { $ref: "#/components/schemas/DeliveryType" }
        thema:
          type: array
          items: { type: string }
        altersgruppe:
          type: array
          items: { type: string }
        cmePoints:
          type: [integer, "null"]
          description: Rendered as "5 CME Punkte" in the card metadata line.
        cmeCategory: { type: [string, "null"] }
        moduleCount: { type: integer, minimum: 0 }
        totalDurationSec:
          type: integer
          minimum: 0
          description: Sum of video durations; formatted client-side.
        enrolment:
          # 3.1 nullability is a type union, not `nullable: true` — which this
          # document's other nullable fields already use and which the SDK
          # generator honours.
          type: ["object", "null"]
          required: [complete]
          description: |
            The **caller's own** standing on this course, or `null` if they
            have not enrolled. Taken from the validated token's user id, never
            from a parameter.

            The card's call to action depends on it: _Zur Fortbildung_ when
            absent, _Fortbildung fortsetzen_ when present and unfinished.

            **Deliberately not a percentage.** A course percentage is the
            output of `rollupProgress` over the whole tree and there is exactly
            one path to it (CLAUDE.md §4 invariant 6); producing it for ten
            cards would mean ten tree builds per request, and the cheap
            approximation that invites would be a second answer to "how far has
            this person got". Both fields here are stored columns.
          properties:
            complete:
              type: boolean
              description: "`completed_at IS NOT NULL` — the course is finished."

    CourseDetail:
      allOf:
        - $ref: "#/components/schemas/CourseSummary"
        - type: object
          required:
            - learningObjectives
            - targetAudience
            - prerequisites
            - vnr
            - accreditationBody
            - fortbildungsnummer
            - organizer
            - eventLocation
            - validFrom
            - validTo
            - requiredWatchPercent
            - passThresholdPercent
            - modules
            - experts
          properties:
            learningObjectives:
              type: array
              items: { type: string }
              description: The "Lernziele" checklist, one entry per bullet.
            targetAudience:
              type: [string, "null"]
              description: The "Zielgruppe" text; newlines are the only formatting.
            prerequisites:
              type: [string, "null"]
              description: |
                The "Vorkenntnisse" paragraph the layout labels separately under
                Zielgruppe (page 02). Its own field so an author does not have to
                remember to type the label into `targetAudience`.
            vnr: { type: [string, "null"] }
            accreditationBody: { type: [string, "null"] }
            fortbildungsnummer:
              type: [string, "null"]
              description: |
                Printed on the Zertifizierung tab as "Fortbildungsnummer: …"
                (layout page 04). Distinct from the VNR, which is the EIV
                credential and never leaves the server.
            organizer: { type: [string, "null"] }
            eventLocation: { type: [string, "null"] }
            validFrom: { type: [string, "null"], format: date-time }
            validTo: { type: [string, "null"], format: date-time }
            requiredWatchPercent:
              type: integer
              minimum: 0
              maximum: 100
              description: |
                The course's actual configured value — the Zertifizierung tab
                renders this, never a hardcoded number (P5-06).
            passThresholdPercent:
              type: integer
              minimum: 0
              maximum: 100
            modules:
              type: array
              items: { $ref: "#/components/schemas/ModuleSummary" }
            experts:
              type: array
              items: { $ref: "#/components/schemas/CourseExpert" }

    ModuleSummary:
      type: object
      required: [id, ordinal, title, subtitle, chapters]
      properties:
        id: { type: string, format: uuid }
        ordinal: { type: integer }
        title: { type: string }
        subtitle: { type: [string, "null"] }
        chapters:
          type: array
          items: { $ref: "#/components/schemas/ChapterSummary" }

    ChapterSummary:
      type: object
      required: [id, ordinal, title, contents]
      properties:
        id: { type: string, format: uuid }
        ordinal: { type: integer }
        title: { type: string }
        contents:
          type: array
          items: { $ref: "#/components/schemas/ContentSummary" }

    ContentSummary:
      type: object
      description: |
        What a course *contains*, for browsing — never what is inside it.

        Deliberately has no field capable of carrying a quiz answer (P4-01),
        and no `fileUrl`, `videoUrl` or `body`. This response is ungated: any
        holder of a tenant token can read it, finished or not. A URL here would
        make both padlocks in the product decorative at once — the Mediathek's,
        which withholds `fileUrl` until the module is complete, and the
        player's, which withholds `videoUrl` until the chapter is reachable.

        The gated shapes are `Material` and `LessonContent`, and they are the
        only ones that carry a URL.
      required: [id, ordinal, kind, title, durationSec, mimeType]
      properties:
        id: { type: string, format: uuid }
        ordinal: { type: integer }
        kind: { $ref: "#/components/schemas/ContentKind" }
        title: { type: string }
        durationSec: { type: [integer, "null"] }
        mimeType:
          type: [string, "null"]
          description: Metadata for an icon. Not a URL — see the description above.

    CourseExpert:
      type: object
      required: [id, ordinal, roleLabel, name, institution, biography, photoUrl]
      properties:
        id: { type: string, format: uuid }
        ordinal: { type: integer }
        roleLabel: { type: string }
        name: { type: string }
        institution: { type: [string, "null"] }
        biography: { type: [string, "null"] }
        photoUrl: { type: [string, "null"] }

    FacetCount:
      type: object
      required: [value, count]
      properties:
        value: { type: string }
        count: { type: integer }

    GateStatus:
      type: string
      description: |
        Server-decided access state. `locked` renders the padlock; the client
        never computes this.
      enum: [locked, available, completed]

    ProgressSummary:
      type: object
      required: [status, completedCount, totalCount, percent]
      properties:
        status:
          type: string
          enum: [not_started, in_progress, completed]
        completedCount: { type: integer, minimum: 0 }
        totalCount: { type: integer, minimum: 0 }
        percent: { type: integer, minimum: 0, maximum: 100 }
        watchedPercent:
          type: integer
          minimum: 0
          maximum: 100
          description: Video content only. Union coverage, not furthest position.
        scorePercent:
          type: integer
          minimum: 0
          maximum: 100
          description: Quiz content only; best scored attempt.

    ContentState:
      type: object
      required: [id, gate, progress]
      properties:
        id: { type: string, format: uuid }
        gate: { $ref: "#/components/schemas/GateStatus" }
        progress: { $ref: "#/components/schemas/ProgressSummary" }

    ChapterState:
      type: object
      required: [id, gate, progress, contents]
      properties:
        id: { type: string, format: uuid }
        gate: { $ref: "#/components/schemas/GateStatus" }
        blockedBy:
          type: string
          format: uuid
          description: |
            The first incomplete chapter standing in the way, so the widget can
            say which one to finish rather than only "locked".
        progress: { $ref: "#/components/schemas/ProgressSummary" }
        contents:
          type: array
          items: { $ref: "#/components/schemas/ContentState" }

    ModuleState:
      type: object
      required: [id, gate, progress, chapters]
      properties:
        id: { type: string, format: uuid }
        gate: { $ref: "#/components/schemas/GateStatus" }
        progress: { $ref: "#/components/schemas/ProgressSummary" }
        chapters:
          type: array
          items: { $ref: "#/components/schemas/ChapterState" }

    CompletionCondition:
      type: string
      description: A condition still standing between the learner and the points.
      enum: [watch, quiz, evaluation, efn]

    EnrolmentState:
      type: object
      description: |
        Everything the player screen renders: the progress ring, the module
        tree with its lock states, the resume target, and what remains.
      required:
        - enrolmentId
        - courseSlug
        - requiredWatchPercent
        - passThresholdPercent
        - achievedWatchPercent
        - quizPassed
        - evaluationSubmitted
        - efnPresent
        - complete
        - outstanding
        - completedAt
        - progress
        - moduleCompletion
        - modules
        - resumeContentId
      properties:
        enrolmentId: { type: string, format: uuid }
        courseSlug: { type: string }
        requiredWatchPercent:
          type: integer
          minimum: 0
          maximum: 100
          description: Snapshotted at enrolment, not read live from the course.
        passThresholdPercent: { type: integer, minimum: 0, maximum: 100 }
        achievedWatchPercent:
          type: integer
          minimum: 0
          maximum: 100
          description: Union coverage across all video content in the course.
        quizPassed: { type: boolean }
        evaluationSubmitted: { type: boolean }
        efnPresent:
          type: boolean
          description: Whether an EFN is on file. The EFN itself is never returned.
        complete: { type: boolean }
        outstanding:
          type: array
          items: { $ref: "#/components/schemas/CompletionCondition" }
        completedAt: { type: [string, "null"], format: date-time }
        progress:
          $ref: "#/components/schemas/ProgressSummary"
        moduleCompletion:
          type: object
          description: Feeds "Sie haben X von Y Modulen abgeschlossen".
          required: [completed, total]
          properties:
            completed: { type: integer, minimum: 0 }
            total: { type: integer, minimum: 0 }
        modules:
          type: array
          items: { $ref: "#/components/schemas/ModuleState" }
        resumeContentId:
          type: [string, "null"]
          format: uuid
          description: |
            Where "Fortbildung fortsetzen" jumps to — the first unfinished
            content the learner may currently reach. Null when nothing is left.

    ContentKind:
      type: string
      enum: [video, text, quiz, details, material]

    MediaSource:
      type: object
      description: |
        One playable rendition. A browser takes the first `<source>` whose
        `type` it can play and silently skips the rest, so the **order of the
        list is the format negotiation** — adaptive streams first, so Safari
        gets HLS and everything else falls through to the progressive file with
        no user-agent detection anywhere. `orderSources` in `@ds/domain` decides
        that order; a client renders the list as given.
      required: [url, mimeType, label]
      properties:
        url:
          type: string
          description: |
            Already resolved — signed if it lives in our storage, passed through
            if the customer serves it. Short-lived, like every other media URL
            in this contract.
        mimeType:
          type: string
          description: |
            Validated on write against a closed list. A `type` no browser
            recognises produces a source that is skipped in silence, which
            presents as a video that will not play with nothing in the console.
        label:
          type: [string, "null"]
          description: Free text for the quality menu — "720p". Null when unlabelled.

    LessonContent:
      type: object
      description: |
        A lesson's actual payload. The **only** shape carrying media URLs or
        `body`, and reachable only through the sequence gate — see the
        `getLesson` description.
      required:
        [
          id,
          kind,
          title,
          durationSec,
          sources,
          posterUrl,
          captionsUrl,
          body,
          lastPositionSec,
          resumeAtSec,
          seekCeilingSec,
          watchedPercent,
          watchedSegments,
        ]
      properties:
        id: { type: string, format: uuid }
        kind: { $ref: "#/components/schemas/ContentKind" }
        title: { type: string }
        durationSec: { type: [integer, "null"] }
        sources:
          type: array
          description: |
            Empty for anything that is not playable media, and for a video whose
            renditions could not be resolved — an `s3://` key belonging to
            another customer resolves to nothing rather than to an error, the
            same degradation the single URL used to have.
          items: { $ref: "#/components/schemas/MediaSource" }
        posterUrl:
          type: [string, "null"]
          description: |
            Still frame shown before playback. Without one the element renders a
            black rectangle until the first frame decodes, and the layout's
            centred play button sits on nothing.
        captionsUrl:
          type: [string, "null"]
          description: |
            WebVTT captions for the video, signed with the same lifetime as
            `videoUrl`. WCAG 1.2.2 (Captions, Prerecorded) is **Level A**, and
            EN 301 549 makes it the reference standard here — a physician who
            cannot hear the video cannot earn the points, and the watch gate
            will record that they did not. `null` when the author supplied none.
        body:
          type: [string, "null"]
          description: Text lessons. Trusted authored content, not learner input.
        lastPositionSec:
          type: integer
          minimum: 0
          description: |
            Where the learner left off, as last reported and stored. Playback
            does not start here — see `resumeAtSec`.
        resumeAtSec:
          type: integer
          minimum: 0
          description: |
            Where playback should start — `lastPositionSec` rewound to the
            containing minute. A learner who left at 14:35 resumes at 14:00.

            Never greater than `seekCeilingSec`: the position playback opens at
            is always one seeking is allowed to reach.

            Decided by the server rather than the player, so every host rewinds
            by the same amount. The replay costs the compliance record nothing:
            coverage is a union of intervals, so re-watching the same seconds
            cannot inflate the percentage.
        seekCeilingSec:
          type: integer
          minimum: 0
          description: |
            The furthest second the player may seek to — the end of what has
            actually been watched, plus a small tolerance.

            Forward seeking past this is what would make the watch gate
            decorative. Seeking backwards is unrestricted; re-watching is
            legitimate and free.
        watchedPercent:
          type: integer
          minimum: 0
          maximum: 100
          description: |
            The server's figure for this content, so the player shows the same
            number the completion gate uses.
        watchedSegments:
          type: array
          description: |
            The **merged** intervals this learner has watched, so the scrub bar
            can shade what has been covered and survive a reload doing it.

            Returned rather than recomputed on the client for the reason the
            percentage is: these are the very intervals `watchedPercent` was
            derived from, so the bar and the number cannot tell different
            stories. They are the caller's own data and disclose nothing — the
            endpoint is already behind the sequence gate.
          items: { $ref: "#/components/schemas/WatchedSegment" }

    WatchedSegment:
      type: object
      description: One played interval, in seconds from the start of the media.
      required: [startSec, endSec]
      properties:
        startSec: { type: number, minimum: 0 }
        endSec: { type: number, minimum: 0 }

    ProgressReport:
      type: object
      required: [segments]
      properties:
        segments:
          type: array
          maxItems: 500
          items: { $ref: "#/components/schemas/WatchedSegment" }
        lastPositionSec:
          type: number
          minimum: 0
          description: Playhead position, for resuming. Never a gate input.

    RejectedSegment:
      type: object
      required: [segment, reason]
      properties:
        segment: { $ref: "#/components/schemas/WatchedSegment" }
        reason:
          type: string
          description: |
            Exactly `SegmentRejectionReason` from the compliance core — the
            contract mirrors the domain's vocabulary rather than inventing a
            parallel one that could drift from it.
          enum:
            [
              not_finite,
              negative,
              zero_or_reversed,
              beyond_duration,
              faster_than_wallclock,
            ]

    ProgressResult:
      type: object
      description: The server's own recomputed figures, never the client's.
      required:
        [
          contentId,
          watchedPercent,
          status,
          accepted,
          rejected,
          watchedSegments,
          seekCeilingSec,
        ]
      properties:
        contentId: { type: string, format: uuid }
        watchedPercent: { type: integer, minimum: 0, maximum: 100 }
        status:
          type: string
          enum: [not_started, in_progress, completed]
        accepted:
          type: integer
          minimum: 0
          description: How many reported intervals were counted.
        rejected:
          type: array
          description: |
            Named rather than silently dropped, so a client bug or a tampering
            attempt is visible.
          items: { $ref: "#/components/schemas/RejectedSegment" }
        watchedSegments:
          type: array
          description: |
            The merged union after this report, so the player's coverage bar
            redraws from the server's answer rather than from what it believed
            it had sent. The difference is visible whenever a segment was
            rejected: the client's optimistic bar would show credit the gate did
            not give.
          items: { $ref: "#/components/schemas/WatchedSegment" }
        seekCeilingSec:
          type: number
          minimum: 0
          description: |
            The furthest second the player may seek to after this report.

            Returned so the player's limit advances as the learner watches. The
            alternative — deriving it on the client from `watchedSegments` —
            would put a rule that decides what counts towards a CME point in two
            implementations, which is what invariant 6 exists to prevent.

    Material:
      type: object
      description: |
        One downloadable item. `fileUrl` is **null while locked** — the gate is
        the absent URL, not the boolean.
      required: [id, title, locked, fileUrl, mimeType, fileSize]
      properties:
        id: { type: string, format: uuid }
        title: { type: string }
        locked: { type: boolean }
        fileUrl: { type: [string, "null"] }
        mimeType: { type: [string, "null"] }
        fileSize: { type: [integer, "null"] }

    MaterialGroup:
      type: object
      description: The layout groups downloads under "Materialien zu Modul N".
      required: [moduleId, moduleTitle, ordinal, locked, materials]
      properties:
        moduleId: { type: string, format: uuid }
        moduleTitle: { type: string }
        ordinal: { type: integer }
        locked: { type: boolean }
        materials:
          type: array
          items: { $ref: "#/components/schemas/Material" }

    MaterialLibrary:
      type: object
      required: [courseSlug, groups]
      properties:
        courseSlug: { type: string }
        groups:
          type: array
          items: { $ref: "#/components/schemas/MaterialGroup" }

    QuizOption:
      type: object
      description: No correctness marker — the shape has nowhere to put one.
      required: [id, ordinal, label]
      properties:
        id: { type: string, format: uuid }
        ordinal: { type: integer }
        label: { type: string }

    QuizQuestion:
      type: object
      required: [id, ordinal, kind, prompt, options]
      properties:
        id: { type: string, format: uuid }
        ordinal: { type: integer }
        kind:
          type: string
          enum: [single, multi]
        prompt: { type: string }
        options:
          type: array
          items: { $ref: "#/components/schemas/QuizOption" }

    Quiz:
      type: object
      required: [contentId, passThresholdPercent, attemptsUsed, maxAttempts, questions]
      properties:
        contentId: { type: string, format: uuid }
        passThresholdPercent: { type: integer, minimum: 0, maximum: 100 }
        attemptsUsed: { type: integer, minimum: 0 }
        maxAttempts:
          type: [integer, "null"]
          description: Null means unlimited, which is the MEDICE configuration.
        questions:
          type: array
          items: { $ref: "#/components/schemas/QuizQuestion" }

    QuizAnswer:
      type: object
      required: [questionId, selectedOptionIds]
      properties:
        questionId: { type: string, format: uuid }
        selectedOptionIds:
          type: array
          items: { type: string, format: uuid }

    QuizSubmission:
      type: object
      required: [answers]
      properties:
        answers:
          type: array
          items: { $ref: "#/components/schemas/QuizAnswer" }

    QuizAttemptResult:
      type: object
      required:
        [
          attemptNumber,
          correctCount,
          totalCount,
          scorePercent,
          passed,
          passThresholdPercent,
        ]
      properties:
        attemptNumber: { type: integer, minimum: 1 }
        correctCount: { type: integer, minimum: 0 }
        totalCount: { type: integer, minimum: 0 }
        scorePercent: { type: integer, minimum: 0, maximum: 100 }
        passed: { type: boolean }
        passThresholdPercent: { type: integer, minimum: 0, maximum: 100 }
        perQuestion:
          type: object
          additionalProperties: { type: boolean }
          description: |
            Present only when the course sets `revealCorrectAnswers`, which a
            CME-certified course never does.

    EvaluationQuestion:
      type: object
      required: [id, ordinal, kind, prompt, required, options]
      properties:
        id: { type: string, format: uuid }
        ordinal: { type: integer }
        kind:
          type: string
          enum: [scale, single, multi, text]
        prompt: { type: string }
        required: { type: boolean }
        options:
          type: array
          items: { type: string }

    Evaluation:
      type: object
      required: [courseSlug, submitted, questions]
      properties:
        courseSlug: { type: string }
        submitted: { type: boolean }
        questions:
          type: array
          items: { $ref: "#/components/schemas/EvaluationQuestion" }

    EvaluationSubmission:
      type: object
      required: [answers]
      properties:
        answers:
          type: array
          items:
            type: object
            required: [evaluationId, answer]
            properties:
              evaluationId: { type: string, format: uuid }
              answer:
                description: Scale number, chosen option(s), or free text.
                oneOf:
                  - type: string
                  - type: number
                  - type: array
                    items: { type: string }

    Certificate:
      type: object
      description: |
        Exactly the fields the Bescheid requires a Teilnahmebescheinigung to
        carry, plus the pre-templated creditability sentence so no client ever
        hardcodes a points figure.
      required:
        - vnr
        - courseTitle
        - completedAt
        - eventLocation
        - organizer
        - cmePoints
        - cmeCategory
        - accreditationBody
        - participantName
        - scientificLeadName
        - creditSentence
      properties:
        vnr: { type: string }
        courseTitle: { type: string }
        completedAt:
          type: string
          format: date-time
          description: |
            Both the Veranstaltungs-datum and -uhrzeit. For an on-demand course
            this is the moment the learner completed it — the same instant that
            starts the EIV reporting window, so certificate and Punktemeldung
            agree by construction.
        eventLocation: { type: string }
        organizer: { type: string }
        cmePoints: { type: integer }
        cmeCategory: { type: string }
        accreditationBody: { type: string }
        participantName: { type: string }
        participantAddress:
          type: string
          description: |
            On the Muster but not in the Bescheid's minimum list, so the
            platform does not collect it and the line renders blank.
        scientificLeadName: { type: string }
        creditSentence: { type: string }

    CompletionInput:
      type: object
      description: |
        The Punktemeldung form, as one request.

        Layout page 13 draws one screen — Titel, Vorname, Nachname, EFN and a
        consent checkbox — behind one button, **Daten übermitteln**. Splitting
        that into three calls would give a physician three ways to end up
        half-submitted: an EFN stored against a course never completed, or a
        completion queued before the consent that authorises it.

        Everything here is optional at the schema level so a host that is not
        this widget can still complete a course on a profile name. What is not
        optional is coherence: Vorname and Nachname are supplied together, and a
        Titel alone is refused.

        `PUT /courses/{slug}/efn` still exists, for correcting an EFN before the
        submission window closes. It is not this.
      properties:
        attestedTitle:
          type: string
          minLength: 1
          maxLength: 100
          description: |
            "Dr. med.", "Prof. Dr." — the layout's `Titel` select. Optional
            even though the layout marks it required, because that select
            offers no empty-but-valid choice and a physician without a title
            still has to be able to finish.
        attestedGivenName:
          type: string
          minLength: 1
          maxLength: 100
          description: Vorname. Supplied together with `attestedFamilyName`.
        attestedFamilyName:
          type: string
          minLength: 1
          maxLength: 100
          description: Nachname. Supplied together with `attestedGivenName`.
        efn:
          type: string
          pattern: "^[0-9]{15}$"
          description: |
            The Einheitliche Fortbildungsnummer, arriving with the rest of the
            form rather than through a separate request.

            **The layout says eighteen digits** ("Die 18-stellige EFN") and its
            placeholder is eighteen characters long. The platform validates
            fifteen, which is what the EIV requirements were written from.
            Unresolved — see S21 in docs/show-stoppers.md. A validator wrong in
            either direction fails where the learner cannot see it.
        consentDocument:
          type: string
          minLength: 1
          maxLength: 200
          description: |
            The privacy notice the learner agreed to, by version, when they
            ticked the consent box on layout page 13.

            Stored with the timestamp of the completion it authorises. GDPR
            Art. 7(1) puts the burden of demonstrating consent on the
            controller, and a boolean would record only that somebody agreed to
            something — consent to the January wording is not consent to the
            June wording.

    Branding:
      type: object
      description: |
        Every field optional. An absent or invalid value falls back to the
        widget's own default, which is always valid — a malformed branding
        record must not be able to break a learner's screen.

        Values are validated against strict grammars in `@ds/domain` before
        storage **and** on read, because each one ends up inside a CSS
        declaration on a page that holds a bearer token. Invalid values are
        dropped, never repaired.
      properties:
        logoUrl:
          type: string
          description: HTTPS only. Accepted only together with `logoAlt`.
        logoAlt:
          type: string
          maxLength: 200
          description: |
            Required whenever a logo is set. A logo a screen reader announces
            as "image" is worse than no logo (CLAUDE.md §3).
        primaryColor:
          type: string
          pattern: "^#(?:[0-9a-fA-F]{3}|[0-9a-fA-F]{6}|[0-9a-fA-F]{8})$"
        primaryContrastColor:
          type: string
          pattern: "^#(?:[0-9a-fA-F]{3}|[0-9a-fA-F]{6}|[0-9a-fA-F]{8})$"
        accentColor:
          type: string
          pattern: "^#(?:[0-9a-fA-F]{3}|[0-9a-fA-F]{6}|[0-9a-fA-F]{8})$"
        fontFamily:
          type: string
          maxLength: 200
          description: |
            A CSS font stack, e.g. `Inter, system-ui, sans-serif`. **Not a
            URL.** Characters that could terminate a CSS declaration are
            rejected, so `url(` cannot be expressed.
        fontFamilyName:
          type: string
          maxLength: 64
          pattern: "^[A-Za-z0-9 _-]{1,64}$"
          description: |
            The family name of the customer's **uploaded** font, if they have
            one. Set by the server from the project row, never by a client: it
            has to match what the upload stored, or the `@font-face` rule names
            a family nothing declares.

            Present only together with `fontVersion`. When both are present the
            uploaded family goes first in the CSS stack, so `fontFamily` is what
            the browser uses while the file downloads or if it fails.
        fontVersion:
          type: string
          maxLength: 40
          description: |
            Cache-busting token for `GET /branding/font` — the upload
            timestamp. Present only together with `fontFamilyName`.
        cornerRadiusPx:
          type: integer
          minimum: 0
          maximum: 24

    FontUpload:
      type: object
      description: |
        A white-label font. See `PUT /admin/branding/font` for why the declared
        type is not believed and why SVG cannot be expressed here at all.
      required: [fontBase64, fontFamilyName]
      properties:
        fontBase64:
          type: string
          maxLength: 2800000
          description: The file, base64, without a `data:` prefix. 2 MB decoded.
        fontMime:
          type: string
          enum: [font/woff2, font/woff]
          description: |
            Optional, and only ever cross-checked against the sniffed format. A
            mismatch is a 422; an absent value is fine.
        fontFamilyName:
          type: string
          pattern: "^[A-Za-z0-9 _-]{1,64}$"
          description: |
            What the CSS will call this family. Narrow because it is emitted
            inside an `@font-face` block, where a brace ends the rule.

    FontState:
      type: object
      description: |
        Metadata about the stored font. Never the bytes — those come from
        `GET /branding/font`.
      required: [fontFamilyName, fontVersion, fontBytes]
      properties:
        fontFamilyName:
          type: string
          nullable: true
        fontVersion:
          type: string
          nullable: true
          description: The upload timestamp; append as `?v=` to the font URL.
        fontBytes:
          type: integer
          nullable: true
          minimum: 0

    AdminCourseSummary:
      type: object
      description: |
        A course as an admin sees it. Carries no secret and no image bytes —
        only whether one is stored. A field that can be read is a field that
        can leak, and one of them authenticates DigitalSpital to a legally
        binding accreditation interface.
      required:
        [
          slug,
          title,
          description,
          deliveryType,
          thema,
          altersgruppe,
          learningObjectives,
          targetAudience,
          prerequisites,
          heroImageUrl,
          fortbildungsnummer,
          validFrom,
          validTo,
          vnr,
          cmePoints,
          cmeCategory,
          requiredWatchPercent,
          passThresholdPercent,
          enrolmentCount,
          completedCount,
          certificateReady,
          missingCertificateFields,
        ]
      properties:
        slug: { type: string }
        title: { type: string }
        description: { type: [string, "null"] }
        deliveryType:
          type: string
          enum: [on_demand, live, praesenz]
          description: Which catalogue tab the course appears under.
        thema:
          type: array
          items: { type: string }
          description: |
            Filter facets, free text rather than a fixed taxonomy — the set
            differs per customer and a shared enum would need migrating every
            time somebody added one.
        altersgruppe:
          type: array
          items: { type: string }
        learningObjectives:
          type: array
          items: { type: string }
          description: The Lernziele checklist, in the order the layout draws it.
        targetAudience:
          type: [string, "null"]
          description: |
            Zielgruppe. Newlines preserved. The Vorkenntnisse sentence is
            **not** part of this any more — the layout labels it separately, so
            it has its own field.
        prerequisites:
          type: [string, "null"]
          description: The "Vorkenntnisse" paragraph under Zielgruppe (layout page 02).
        heroImageUrl: { type: [string, "null"] }
        fortbildungsnummer: { type: [string, "null"] }
        validFrom: { type: [string, "null"], format: date-time }
        validTo: { type: [string, "null"], format: date-time }
        vnr: { type: [string, "null"] }
        cmePoints: { type: [integer, "null"] }
        cmeCategory: { type: [string, "null"] }
        requiredWatchPercent: { type: integer, minimum: 0, maximum: 100 }
        passThresholdPercent: { type: integer, minimum: 0, maximum: 100 }
        enrolmentCount: { type: integer, minimum: 0 }
        completedCount: { type: integer, minimum: 0 }
        certificateReady:
          type: boolean
          description: |
            Whether this course could issue a valid Teilnahmebescheinigung
            today. Computed with the same rule the certificate endpoint
            enforces.
        missingCertificateFields:
          type: array
          items: { type: string }

    AdminCourseDetail:
      allOf:
        - $ref: "#/components/schemas/AdminCourseSummary"
        - type: object
          required:
            [
              organizer,
              eventLocation,
              accreditationBody,
              scientificLeadName,
              scientificLeadTitle,
              certificateIssuePlace,
              hasStampImage,
              hasSignatureImage,
              hasVnrPassword,
              maxQuizAttempts,
              revealCorrectAnswers,
            ]
          properties:
            organizer: { type: [string, "null"] }
            eventLocation: { type: [string, "null"] }
            accreditationBody: { type: [string, "null"] }
            scientificLeadName: { type: [string, "null"] }
            scientificLeadTitle: { type: [string, "null"] }
            certificateIssuePlace: { type: [string, "null"] }
            hasStampImage:
              type: boolean
              description: Presence only. The bytes are never returned.
            hasSignatureImage: { type: boolean }
            hasVnrPassword:
              type: boolean
              description: |
                The only readable trace of the VNR password anywhere in the
                API. Not the value, not a mask, not a length.
            maxQuizAttempts: { type: [integer, "null"] }
            revealCorrectAnswers: { type: boolean }

    AdminCourseUpdate:
      type: object
      description: |
        A PATCH: every field optional, an absent field means "leave alone".
        `null` clears a nullable text field.
      properties:
        title: { type: string, minLength: 1, maxLength: 300 }
        description: { type: [string, "null"], maxLength: 5000 }
        deliveryType: { type: string, enum: [on_demand, live, praesenz] }
        thema:
          type: array
          maxItems: 20
          items: { type: string, minLength: 1, maxLength: 100 }
        altersgruppe:
          type: array
          maxItems: 20
          items: { type: string, minLength: 1, maxLength: 100 }
        learningObjectives:
          type: array
          maxItems: 30
          items: { type: string, minLength: 1, maxLength: 500 }
        targetAudience: { type: [string, "null"], maxLength: 5000 }
        prerequisites: { type: [string, "null"], maxLength: 2000 }
        heroImageUrl: { type: [string, "null"], format: uri, maxLength: 2000 }
        cmePoints: { type: [integer, "null"], minimum: 1, maximum: 100 }
        cmeCategory: { type: [string, "null"], maxLength: 50 }
        fortbildungsnummer: { type: [string, "null"], maxLength: 100 }
        validFrom: { type: [string, "null"], format: date-time }
        validTo: { type: [string, "null"], format: date-time }
        requiredWatchPercent: { type: integer, minimum: 0, maximum: 100 }
        passThresholdPercent: { type: integer, minimum: 0, maximum: 100 }
        organizer: { type: [string, "null"], maxLength: 300 }
        eventLocation: { type: [string, "null"], maxLength: 300 }
        accreditationBody: { type: [string, "null"], maxLength: 300 }
        scientificLeadName: { type: [string, "null"], maxLength: 200 }
        scientificLeadTitle: { type: [string, "null"], maxLength: 100 }
        certificateIssuePlace: { type: [string, "null"], maxLength: 200 }
        vnrPassword:
          type: string
          minLength: 1
          maxLength: 200
          description: |
            Write-only. Encrypted at rest with the application KMS key, never
            returned by any endpoint and never written to a log.
        acknowledgeAccreditationRisk:
          type: boolean
          description: |
            Required to be true when lowering `passThresholdPercent` below the
            accredited minimum. Without it the request is refused with 409.

    CertificateAssetUpload:
      type: object
      description: |
        Base64 without a `data:` prefix. The magic bytes are checked; the
        declared mime type is treated as a claim, not as truth.
      properties:
        stampImageBase64: { type: string, maxLength: 700000 }
        stampImageMime: { type: string, enum: [image/png, image/jpeg] }
        signatureImageBase64: { type: string, maxLength: 700000 }
        signatureImageMime: { type: string, enum: [image/png, image/jpeg] }

    EivState:
      type: string
      description: |
        `needs_attention` is distinct from `failed` on purpose: a submission
        unresolved after the fast retries will not fix itself, and the paper
        fallback is only open while the 8-day deadline has not passed.
      enum: [none, queued, submitted, failed, needs_attention, abandoned]

    ParticipantRow:
      type: object
      description: |
        One participant's status. The EFN itself never appears — only whether
        one is on file (ADR-0004).
      required:
        [
          enrolmentId,
          participantName,
          email,
          efnPresent,
          watchedPercent,
          quizPassed,
          evaluationSubmitted,
          progressPercent,
          complete,
          completedAt,
          eivState,
          eivAttempts,
          eivReportDueAt,
          certificateState,
        ]
      properties:
        enrolmentId: { type: string, format: uuid }
        participantName: { type: string }
        email: { type: [string, "null"] }
        efnPresent: { type: boolean }
        watchedPercent: { type: integer, minimum: 0, maximum: 100 }
        quizPassed: { type: boolean }
        evaluationSubmitted: { type: boolean }
        progressPercent: { type: integer, minimum: 0, maximum: 100 }
        complete: { type: boolean }
        completedAt: { type: [string, "null"], format: date-time }
        eivState: { $ref: "#/components/schemas/EivState" }
        eivAttempts: { type: integer, minimum: 0 }
        eivReportDueAt: { type: [string, "null"], format: date-time }
        certificateState:
          type: string
          enum: [none, pending, issued, delivered, bounced]

    ParticipantList:
      type: object
      required: [courseSlug, rows]
      properties:
        courseSlug: { type: string }
        rows:
          type: array
          items: { $ref: "#/components/schemas/ParticipantRow" }

    EfnInput:
      type: object
      required: [efn]
      properties:
        efn:
          type: string
          pattern: "^[0-9]{15}$"
          description: 15 digits. Never returned by any endpoint once stored.

    CourseListResponse:
      type: object
      required: [items, page, perPage, total, facets]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/CourseSummary" }
        page: { type: integer, minimum: 1 }
        perPage: { type: integer, minimum: 1 }
        total: { type: integer, minimum: 0 }
        facets:
          type: object
          required: [thema, altersgruppe]
          properties:
            thema:
              type: array
              items: { $ref: "#/components/schemas/FacetCount" }
            altersgruppe:
              type: array
              items: { $ref: "#/components/schemas/FacetCount" }

    # -----------------------------------------------------------------------
    # Authoring (P9-02, P9-04, P9-05)
    #
    # Two families of shape here, and the difference is deliberate.
    #
    # *Read* shapes (`CourseStructure`, `AuthoringQuiz`, `AuthoringEvaluation`)
    # carry counts of learner records alongside the content. The console needs
    # them to disable deletion **and say why** before an author clicks, rather
    # than after.
    #
    # *Write* shapes carry no `ordinal` field anywhere. Position is expressed
    # by position in an array. A client that can set ordinals directly can set
    # two siblings to the same one, and `UNIQUE (parent_id, ordinal)` then turns
    # an authoring mistake into a 500.
    # -----------------------------------------------------------------------

    Slug:
      type: string
      pattern: "^[a-z0-9-]{1,100}$"
      description: |
        Lowercase letters, digits and hyphens. Must not begin or end with a
        hyphen — checked separately rather than folded into the pattern, since
        the natural regex for it nests quantifiers.

    DepartmentSummary:
      type: object
      required: [slug, name, projectCount]
      properties:
        slug: { type: string }
        name: { type: string }
        projectCount:
          type: integer
          minimum: 0
          description: Projects under this department, in this tenant.

    LearnerRecord:
      type: object
      required:
        [
          userId,
          enrolmentId,
          courseSlug,
          courseTitle,
          attestedName,
          maskedEfn,
          watchedPercent,
          quizBestPercent,
          completedAt,
          submissionStage,
          certificateStatus,
        ]
      properties:
        userId: { type: string, format: uuid }
        enrolmentId: { type: string, format: uuid }
        courseSlug: { type: string }
        courseTitle: { type: string }
        attestedName:
          type: [string, "null"]
          description: The name the physician attested for their certificate.
        maskedEfn:
          type: [string, "null"]
          description: |
            Last four digits only, the rest replaced with bullets. The whole
            value is never returned to any staff role (ADR-0004).
        watchedPercent: { type: integer, minimum: 0, maximum: 100 }
        quizBestPercent: { type: [integer, "null"], minimum: 0, maximum: 100 }
        completedAt: { type: [string, "null"], format: date-time }
        submissionStage:
          type: string
          enum: [none, pending, submitted, abandoned]
          description: |
            How far the Punktemeldung has got. `submitted` is what makes a name
            correction refusable.
        certificateStatus: { type: [string, "null"] }

    CertificateRecord:
      type: object
      required: [id, enrolmentId, participantName, status, issuedAt, deliveredAt]
      properties:
        id: { type: string, format: uuid }
        enrolmentId: { type: string, format: uuid }
        participantName: { type: string }
        status:
          type: string
          enum: [pending, issued, delivered, bounced, revoked]
        issuedAt: { type: [string, "null"], format: date-time }
        deliveredAt: { type: [string, "null"], format: date-time }

    StaffScope:
      type: object
      required: [role, customerId, departmentId]
      description: |
        What an operator may reach. `customerId` is null only for `super_admin`
        — a customer is the tenant boundary, and the database CHECK enforces
        the pairing.
      properties:
        role:
          type: string
          enum: [course_editor, department_admin, customer_admin, super_admin]
        customerId: { type: [string, "null"], format: uuid }
        departmentId: { type: [string, "null"], format: uuid }

    StaffInvitation:
      allOf:
        - $ref: "#/components/schemas/StaffScope"
        - type: object
          required: [email, displayName]
          properties:
            email: { type: string, format: email, maxLength: 320 }
            displayName: { type: string, minLength: 1, maxLength: 200 }

    StaffAccount:
      type: object
      required: [id, email, displayName, disabledAt, lastLoginAt, totpEnrolled, grants]
      description: |
        Carries no password hash and no TOTP secret — only whether a second
        factor is enrolled. When somebody enrolled their authenticator is
        nobody's business but theirs.
      properties:
        id: { type: string, format: uuid }
        email: { type: string }
        displayName: { type: string }
        disabledAt: { type: [string, "null"], format: date-time }
        lastLoginAt: { type: [string, "null"], format: date-time }
        totpEnrolled: { type: boolean }
        grants:
          type: array
          items: { $ref: "#/components/schemas/StaffScope" }

    CustomerSummary:
      type: object
      required: [id, slug, name, createdAt, departmentCount, projectCount, courseCount]
      description: |
        A customer plus how much is inside it. The counts come back with the
        list because the console shows them on the same screen and because a
        follow-up query would be tenant-scoped — counting another customer's
        courses means opening a tenant context on that customer, which is
        precisely what the caller has not been authorised to do.
      properties:
        id:
          type: string
          format: uuid
          description: |
            The customer's own id. Exposed because a grant references it —
            scoping an operator account to a customer needs this value, and
            only a super admin can reach this endpoint at all.
        slug: { $ref: "#/components/schemas/Slug" }
        name: { type: string }
        createdAt: { type: string, format: date-time }
        departmentCount: { type: integer, minimum: 0 }
        projectCount: { type: integer, minimum: 0 }
        courseCount: { type: integer, minimum: 0 }

    CustomerCreate:
      type: object
      required: [slug, name]
      properties:
        slug: { $ref: "#/components/schemas/Slug" }
        name: { type: string, minLength: 1, maxLength: 200 }

    CustomerUpdate:
      type: object
      required: [name]
      description: |
        No slug. See `adminUpdateCustomer` — re-slugging is a rename of the
        thing itself, not a display change.
      properties:
        name: { type: string, minLength: 1, maxLength: 200 }

    DepartmentCreate:
      type: object
      required: [slug, name]
      description: |
        Names no customer. `customer_id` comes from the RLS session variable, so
        there is no field capable of creating a department into another tenant
        (ADR-0002).
      properties:
        slug: { $ref: "#/components/schemas/Slug" }
        name: { type: string, minLength: 1, maxLength: 300 }

    DepartmentUpdate:
      type: object
      properties:
        name: { type: string, minLength: 1, maxLength: 300 }

    ProjectSummary:
      type: object
      description: |
        Carries the Keycloak binding and the SMTP settings, and **never** the
        SMTP password. `hasSmtpPassword` is its only readable trace, exactly as
        `hasVnrPassword` is for a course (CLAUDE.md §4 invariant 7).
      required:
        [
          slug,
          name,
          departmentSlug,
          keycloakIssuer,
          keycloakAudience,
          keycloakRealm,
          smtpHost,
          smtpPort,
          smtpUsername,
          smtpFromAddress,
          smtpFromName,
          hasSmtpPassword,
          branding,
          courseCount,
        ]
      properties:
        slug: { type: string }
        name: { type: string }
        departmentSlug: { type: string }
        keycloakIssuer: { type: [string, "null"] }
        keycloakAudience: { type: [string, "null"] }
        keycloakRealm: { type: [string, "null"] }
        smtpHost: { type: [string, "null"] }
        smtpPort: { type: [integer, "null"] }
        smtpUsername: { type: [string, "null"] }
        smtpFromAddress: { type: [string, "null"] }
        smtpFromName: { type: [string, "null"] }
        hasSmtpPassword:
          type: boolean
          description: Presence only. The ciphertext is never returned.
        branding: { $ref: "#/components/schemas/Branding" }
        courseCount: { type: integer, minimum: 0 }

    ProjectCreate:
      type: object
      required: [departmentSlug, slug, name]
      properties:
        departmentSlug: { $ref: "#/components/schemas/Slug" }
        slug: { $ref: "#/components/schemas/Slug" }
        name: { type: string, minLength: 1, maxLength: 300 }

    ProjectUpdate:
      type: object
      description: |
        Every field optional; absent means unchanged. `smtpPassword` is
        write-only — settable here, returned by nothing.

        The Keycloak binding decides which realm every token for this project is
        validated against (ADR-0003), so a wrong value locks every learner out
        at once. The console confirms before saving it; the API applies what it
        is given, because refusing to let an admin change their own IdP would be
        a worse failure than the one it prevented.
      properties:
        name: { type: string, minLength: 1, maxLength: 300 }
        keycloakIssuer: { type: [string, "null"], format: uri, maxLength: 2000 }
        keycloakAudience: { type: [string, "null"], maxLength: 200 }
        keycloakRealm: { type: [string, "null"], maxLength: 200 }
        smtpHost: { type: [string, "null"], maxLength: 300 }
        smtpPort: { type: [integer, "null"], minimum: 1, maximum: 65535 }
        smtpUsername: { type: [string, "null"], maxLength: 300 }
        smtpPassword:
          type: string
          minLength: 1
          maxLength: 300
          writeOnly: true
          description: |
            Encrypted with the application KMS key before storage. An empty
            string is rejected rather than read as "clear it" — clearing a
            password by sending nothing is too easy to do by accident.
        smtpFromAddress: { type: [string, "null"], format: email, maxLength: 300 }
        smtpFromName: { type: [string, "null"], maxLength: 200 }
        branding:
          type: object
          additionalProperties: true
          description: |
            Send a `Branding`. Typed open rather than as `$ref: Branding`
            because the server is deliberately lenient here: the grammars that
            decide what a valid colour or font stack is live in `@ds/domain`,
            the *parsed* result is what gets stored, and anything unrecognised
            is dropped rather than rejected or repaired. Restating those
            grammars in this document would be a second copy to keep in step.

            What comes back on the next read is the parsed value — see
            `ProjectSummary.branding`.

    CourseCreate:
      type: object
      required: [projectSlug, slug, title]
      description: |
        Deliberately minimal. Everything a certificate needs is set afterwards
        on the settings screen, which already refuses a pass threshold below the
        accredited minimum and reports the missing fields. Duplicating those
        rules into a creation form would be a second place to get them wrong.
      properties:
        projectSlug: { $ref: "#/components/schemas/Slug" }
        slug: { $ref: "#/components/schemas/Slug" }
        title: { type: string, minLength: 1, maxLength: 300 }
        description: { type: [string, "null"], maxLength: 20000 }
        deliveryType: { $ref: "#/components/schemas/DeliveryType" }

    AuthoringContent:
      type: object
      required:
        [
          id,
          kind,
          title,
          body,
          sources,
          posterUrl,
          captionsUrl,
          durationSec,
          fileUrl,
          fileSize,
          mimeType,
          learnerRecords,
          questionCount,
        ]
      properties:
        id: { type: string, format: uuid }
        kind: { $ref: "#/components/schemas/ContentKind" }
        title: { type: string }
        body: { type: [string, "null"] }
        sources:
          type: array
          description: |
            The stored list, **unresolved** — an `s3://` reference is returned as
            written. The console edits what is stored; presigning here would
            hand an author a URL that expires while they are still looking at
            the form, and would put a live capability in a response whose whole
            purpose is editing.
          items: { $ref: "#/components/schemas/MediaSourceWrite" }
        posterUrl: { type: [string, "null"] }
        captionsUrl: { type: [string, "null"] }
        durationSec: { type: [integer, "null"] }
        fileUrl: { type: [string, "null"] }
        fileSize: { type: [integer, "null"] }
        mimeType: { type: [string, "null"] }
        learnerRecords:
          type: integer
          minimum: 0
          description: |
            How many learner records point at this item. Non-zero disables
            deletion in the console and explains itself there, rather than
            letting an author click delete and receive a refusal to interpret.
        questionCount:
          type: [integer, "null"]
          description: Quiz items only. Zero is a quiz nobody can pass.

    AuthoringChapter:
      type: object
      required: [id, title, body, contents]
      properties:
        id: { type: string, format: uuid }
        title: { type: string }
        body: { type: [string, "null"] }
        contents:
          type: array
          items: { $ref: "#/components/schemas/AuthoringContent" }

    AuthoringModule:
      type: object
      required: [id, title, subtitle, chapters]
      properties:
        id: { type: string, format: uuid }
        title: { type: string }
        subtitle: { type: [string, "null"] }
        chapters:
          type: array
          items: { $ref: "#/components/schemas/AuthoringChapter" }

    AuthoringExpert:
      type: object
      required: [id, roleLabel, name, institution, biography, photoUrl]
      properties:
        id: { type: string, format: uuid }
        roleLabel: { type: string }
        name: { type: string }
        institution: { type: [string, "null"] }
        biography: { type: [string, "null"] }
        photoUrl: { type: [string, "null"] }

    CourseStructure:
      type: object
      description: |
        The whole authoring tree, in author order. Returned by every structure
        mutation as well as the GET, so the console never has to reconstruct
        what the server now holds — and never disagrees with it.
      required: [courseSlug, title, modules, experts]
      properties:
        courseSlug: { type: string }
        title: { type: string }
        modules:
          type: array
          items: { $ref: "#/components/schemas/AuthoringModule" }
        experts:
          type: array
          items: { $ref: "#/components/schemas/AuthoringExpert" }

    ModuleWrite:
      type: object
      required: [title]
      properties:
        title: { type: string, minLength: 1, maxLength: 300 }
        subtitle: { type: [string, "null"], maxLength: 300 }

    ChapterWrite:
      type: object
      required: [title]
      properties:
        title: { type: string, minLength: 1, maxLength: 300 }
        body: { type: [string, "null"], maxLength: 20000 }

    ContentWrite:
      type: object
      required: [kind, title]
      description: |
        Every field is accepted for every kind; the rules are applied by
        `contentProblems` in `@ds/domain` rather than by this document, so there
        is one place that knows a `video` needs a `durationSec` instead of five
        branches here and five more in the console.

        That one is not stylistic: the watch requirement is a percentage of a
        known length, and a video with no length would be skippable while
        appearing to count toward a CME point. It is refused with a 422 naming
        the field.
      properties:
        kind: { $ref: "#/components/schemas/ContentKind" }
        title: { type: string, minLength: 1, maxLength: 300 }
        body: { type: [string, "null"], maxLength: 20000 }
        sources:
          type: array
          maxItems: 10
          description: |
            Playable renditions, in the author's order. Required to be non-empty
            for a `video`; ignored for every other kind. Capped because a
            handful of encodings is the real use and an unbounded list is a
            payload with no reason to be one.
          items: { $ref: "#/components/schemas/MediaSourceWrite" }
        posterUrl: { type: [string, "null"], format: uri, maxLength: 2000 }
        captionsUrl:
          type: [string, "null"]
          format: uri
          maxLength: 2000
          description: |
            WebVTT. Owed for every video carrying speech — WCAG 1.2.2 is Level
            A — but not enforced: a slide-only recording legitimately has none,
            and the server cannot tell the two apart. The console asks for it
            and says why.
        durationSec: { type: [integer, "null"], minimum: 1, maximum: 86400 }
        fileUrl: { type: [string, "null"], format: uri, maxLength: 2000 }
        fileSize: { type: [integer, "null"], minimum: 0 }
        mimeType: { type: [string, "null"], maxLength: 200 }

    MediaSourceWrite:
      type: object
      description: |
        A rendition as an author supplies it. `url` accepts an `s3://` reference
        as well as an absolute URL, which is why it carries no `format: uri` —
        the media resolver, not this document, decides what a stored reference
        may be.
      required: [url, mimeType]
      properties:
        url: { type: string, minLength: 1, maxLength: 2000 }
        mimeType:
          type: string
          maxLength: 100
          description: |
            One of the types `@ds/domain` accepts. Refused with a 422 naming the
            field rather than stored, because a browser skips an unrecognised
            `type` in silence and the author would ship a video nobody can play.
        label: { type: [string, "null"], maxLength: 60 }

    StructureOrder:
      type: object
      description: |
        Ids only — this shape moves things, it never edits them. A chapter
        listed under a different module than it currently sits in **is** the
        move.

        Every level must be a permutation of what the server holds. A list that
        lost an item during a re-render is rejected rather than obeyed; obeying
        it would delete a chapter from a course learners are part-way through.
      required: [modules]
      properties:
        modules:
          type: array
          items:
            type: object
            required: [id, chapters]
            properties:
              id: { type: string, format: uuid }
              chapters:
                type: array
                items:
                  type: object
                  required: [id, contents]
                  properties:
                    id: { type: string, format: uuid }
                    contents:
                      type: array
                      items: { type: string, format: uuid }

    ExpertsWrite:
      type: object
      required: [experts]
      properties:
        experts:
          type: array
          maxItems: 50
          items:
            type: object
            required: [roleLabel, name]
            properties:
              roleLabel: { type: string, minLength: 1, maxLength: 100 }
              name: { type: string, minLength: 1, maxLength: 200 }
              institution: { type: [string, "null"], maxLength: 300 }
              biography: { type: [string, "null"], maxLength: 20000 }
              photoUrl: { type: [string, "null"], format: uri, maxLength: 2000 }

    AuthoringQuiz:
      type: object
      description: |
        A quiz as its author sees it — **with** the answer key. The only shape
        in this document carrying `isCorrect`, and reachable only from an admin
        route. The learner-facing `QuizOption` has no field capable of holding
        it, which is what makes leaking the key a type error rather than
        something a reviewer has to notice.
      required: [contentId, questions]
      properties:
        contentId: { type: string, format: uuid }
        questions:
          type: array
          items:
            type: object
            required: [id, prompt, kind, answerCount, options]
            properties:
              id: { type: string, format: uuid }
              prompt: { type: string }
              kind: { type: string, enum: [single, multi] }
              answerCount:
                type: integer
                minimum: 0
                description: |
                  Answers recorded against this question. Non-zero refuses
                  deletion: an already-submitted attempt has to keep meaning
                  what it meant when it was scored.
              options:
                type: array
                items:
                  type: object
                  required: [id, label, isCorrect]
                  properties:
                    id: { type: string, format: uuid }
                    label: { type: string }
                    isCorrect: { type: boolean }

    QuizWrite:
      type: object
      description: |
        Diffed, not wiped and rewritten. `id` present means "the existing row,
        changed"; absent means "new"; anything the server holds and this
        document does not name is a deletion — refused, with the count, if a
        learner has answered it.

        Two refusals no form should be trusted with: a question with **no**
        correct option cannot be passed by anybody, and a `single` question with
        **two** correct options cannot either, because scoring is exact-set.
      required: [questions]
      properties:
        questions:
          type: array
          maxItems: 100
          items:
            type: object
            required: [prompt, kind, options]
            properties:
              id: { type: string, format: uuid }
              prompt: { type: string, minLength: 1, maxLength: 2000 }
              kind: { type: string, enum: [single, multi] }
              options:
                type: array
                minItems: 2
                maxItems: 10
                items:
                  type: object
                  required: [label, isCorrect]
                  properties:
                    id: { type: string, format: uuid }
                    label: { type: string, minLength: 1, maxLength: 1000 }
                    isCorrect: { type: boolean }

    AuthoringEvaluation:
      type: object
      required: [courseSlug, questions]
      properties:
        courseSlug: { type: string }
        questions:
          type: array
          items:
            type: object
            required: [id, prompt, kind, required, options, responseCount]
            properties:
              id: { type: string, format: uuid }
              prompt: { type: string }
              kind: { type: string, enum: [scale, text, single] }
              required: { type: boolean }
              options:
                type: array
                items: { type: string }
              responseCount:
                type: integer
                minimum: 0
                description: Responses recorded. Non-zero refuses deletion.

    EvaluationWrite:
      type: object
      description: |
        Same diffing rule as the quiz. The Anerkennung requires an evaluation,
        so an empty one is a course that cannot be completed.

        Note for whoever edits this next: `required` and `options` describe
        their fallbacks in prose rather than with `default:`. `openapi-typescript`
        treats a property carrying `default:` as always present — correct for a
        response, wrong for a request body, where the whole point of a default
        is that the field may be omitted. Adding `default:` here makes the
        generated type require a field the server is happy to supply itself,
        and the DTO-parity test fails.
      required: [questions]
      properties:
        questions:
          type: array
          maxItems: 50
          items:
            type: object
            required: [prompt, kind]
            properties:
              id: { type: string, format: uuid }
              prompt: { type: string, minLength: 1, maxLength: 2000 }
              kind: { type: string, enum: [scale, text, single] }
              required:
                type: boolean
                description: Omitted means `true`.
              options:
                type: array
                maxItems: 20
                items: { type: string, minLength: 1, maxLength: 200 }
                description: |
                  Omitted means none. `scale` and `text` questions do not use
                  it; a `single` question without options is one nobody can
                  answer.

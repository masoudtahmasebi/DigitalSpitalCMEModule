/**
 * Admin console entry point (P9-01).
 *
 * Placeholder until the console shell lands. Kept as a real module so the
 * workspace typechecks and the boundary lint rules apply from day one.
 */
export const ADMIN_APP_NAME = "ds-admin";
